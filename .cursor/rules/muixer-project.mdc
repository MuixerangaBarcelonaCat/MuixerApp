---
description: MuixerApp project context, tech stack and architecture decisions
alwaysApply: true
---

# MuixerApp — APPsistència 2.0

Open-source attendance & figure management system for Muixerangues (human tower groups).

## Tech Stack (Decided)

- **Backend:** NestJS + TypeScript (strict)
- **Frontend Dashboard:** Angular 20+ (standalone components, signals, OnPush) + DaisyUI + Angular CDK
- **Mobile:** Angular PWA (no Ionic, no native apps)
- **Database:** PostgreSQL — **Docker local in dev** (`docker compose up`), VPS Docker in prod
- **Monorepo:** Nx workspace (`apps/api`, `apps/dashboard`, `apps/pwa`, `libs/shared`)
- **ORM:** TypeORM (class-based entities with decorators)
- **Styling:** Tailwind CSS v3 + DaisyUI v4 (stable, semantic classes)
- **Docker:** PostgreSQL in dev (`docker-compose.yml`), full stack in prod (`docker-compose.prod.yml` + `apps/api/Dockerfile`)

## Architecture

```
apps/
  api/          → NestJS backend (REST API)
  dashboard/    → Angular admin web (desktop/tablet)
  pwa/          → Angular PWA for members (mobile-first)
libs/
  shared/       → DTOs, interfaces, enums shared between apps
```

## Multi-tenant

Initially built for Muixeranga de Barcelona. Designed to support multiple colles in the future (each with their own branding: logo, colors).

## Data Source

Legacy app at `muixerangadebarcelona.appsistencia.cat` has a PHP API. Data migration scripts extract from there. See `docs/API_APPSISTENCIA.md` for endpoint documentation.

## Current Status

Do not track phase status here — it goes stale. Source of truth:

- **Architecture, conventions, commands:** `CLAUDE.md` (single source of truth for agents)
- **Phases and next increment:** `docs/ROADMAP.md`
- **Open technical debt:** `docs/DEBT.md`
- **Documentation index:** `docs/MAP.md`

## Terminology

- **Membre**: gender-neutral for colla members. Never "casteller".
- **Xicalla**: children (< 16). Never "canalla".
- Code entity: `Person` (not `Casteller`, not `Membre`)

## Key Documentation

- `CLAUDE.md` — architecture, conventions, commands
- `docs/MAP.md` — documentation + code map (regenerate with `pnpm run docs:map`)
- `docs/ROADMAP.md` · `docs/DEBT.md` — status and open debt
- `README.md` — human setup guide
- Swagger `/api/docs` — the only endpoint reference
