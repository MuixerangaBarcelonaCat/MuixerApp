import { PINYES_RENDER_LIB_NAME } from './pinyes-render.constants';

describe('PINYES_RENDER_LIB_NAME', () => {
  it('is the library name', () => {
    expect(PINYES_RENDER_LIB_NAME).toBe('pinyes-render');
  });
});
