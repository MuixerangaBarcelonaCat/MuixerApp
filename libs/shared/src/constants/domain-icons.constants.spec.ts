import { ICON_OBSERVACIONS } from './domain-icons.constants';

describe('ICON_OBSERVACIONS', () => {
  it('is the CircleAlert lucide icon name', () => {
    expect(ICON_OBSERVACIONS).toBe('CircleAlert');
  });
});
