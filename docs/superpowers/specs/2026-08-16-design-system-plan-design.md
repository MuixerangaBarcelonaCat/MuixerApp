# Design System — Build Plan

**Date:** 2026-08-16
**Status:** Draft
**Scope:** Process/infrastructure for building and enforcing a documented design system across `apps/dashboard` and `apps/pwa`. Does **not** define the visual identity itself (colors, type, shape language) — that comes from the user separately and gets poured into the token layer this plan builds.

---

## Problem

The apps currently look generic. Root cause, structurally, not just aesthetically:

- Both apps' entire visual identity reduces to one input — `generateCollaTheme(primaryHex)` in [tailwind.config.js](../../../tailwind.config.js) — which auto-derives every DaisyUI token from a single color. There is no typography scale, spacing scale, shape language, motion system, or component identity layer beyond stock DaisyUI v4.
- Reusable components exist only for the dashboard (`apps/dashboard/src/app/shared/components/`, documented in [docs/DASHBOARD_UI.md](../../DASHBOARD_UI.md)). The PWA has no equivalent shared layer, so nothing structurally guarantees the two apps look like the same product.
- Nothing prevents drift: no lint rule, no CI check, no living reference stops a raw hex code or an arbitrary Tailwind value from being pasted into a template.
- The canvas rendering layer (`libs/pinyes-render`, Konva-based) sources its own hardcoded colors — not just the documented `figure-palette.util.ts`, but **98 hex literals across the library** (64 alone in `figure-canvas.component.ts`: zone colors, selection/conflict strokes, and a set of attendance-status colors that independently reinvent values DaisyUI's `success`/`warning`/`error` tokens already define). CSS custom properties don't reach Konva draw calls, so even a perfect token layer in CSS wouldn't automatically cover canvas-drawn content. Full breakdown: Phase 1 appendix, §1.4.

None of this is a "pick better colors" problem. It's a "there is no system, only a single knob" problem. This plan builds the system; a separate pass (once the visual identity is defined) fills it in.

## Goal

A design system that is:

1. **Token-driven** — every visual decision (color, type, spacing, radius, shadow, motion) traces to one named, documented source, in both light and dark variants.
2. **Shared** — one component library consumed by both `apps/dashboard` and `apps/pwa`, not two parallel implementations.
3. **Documented** — a single canonical doc (`docs/DESIGN_SYSTEM.md`) plus a live, in-app reference that can't silently go stale.
4. **Enforced** — drift (raw hex, ad hoc spacing, one-off components) is structurally hard, ideally CI-blocked, not just "please don't."



## Non-goals

- Choosing the actual palette, typography, or "personality" — that's the user's call, fed into Phase 2 once this plan is approved.
- New product features. This is purely presentation-layer infrastructure.

**Explicitly in scope, called out because it's easy to assume otherwise:** the Konva canvas (`libs/pinyes-render`) is not exempt. Its palette (the qualitative figure-distinguishing colors, panel background, shadow) and node styling are designed as part of the same identity, not left as a separate hardcoded set that's merely re-plumbed to read tokens. See 2.1/2.3 and 7.2.

---



## Identity principles (resolved)

Set by the user while designing Button, once there was a real component to ground them against rather than inventing prose in the abstract (per Phase 3.3's deferral). Three principles, grounding every subsequent personality decision rather than re-litigating tone per component:

1. **Paper quality, restrained.** The identity should feel like a *mestre* (leader) sketching pinyes (figures) in a notebook before rehearsal — hand-made, drawn, not printed — without tipping into pastiche (no script fonts, no illustrated corners everywhere). It lands sparingly, on elements that can carry it without hurting legibility or turning noisy (e.g. a large flat surface's grain, a sketched-vs-inked distinction between an unfinished and a committed state) — not applied uniformly to every element, and never at the cost of the APCA contrast work already done in Phase 2.
2. **Not too serious.** Small gimmicks are welcome as long as they never block or slow down a real task — personality expressed through motion and small detail, not through friction.
3. **Distinctly Valencian and about muixerangues, not generic.** The identity should read as *specifically* about muixerangues (traditional Valencian human towers), not a reskinned generic SaaS app. The sash (*faixa*) — the textured diagonal weave already built into the token layer (`--ds-sash-`*) — is the first concrete motif; more small motifs in this vein are expected as later components are designed, not invented all at once here.

Where these already show up in decisions made so far: the warm paper/ink OKLCH palette (not stark black-and-white), Fraunces for display headings (a serif with more character than a generic UI sans), Atkinson Hyperlegible Next specifically for canvas name labels (a practical, non-decorative nod to legibility at distance during a real rehearsal), and the sash's two-tone weave texture. Not every component needs to carry all three at once — Button (§3, Tier 1) is deliberately restrained, because a small interactive control isn't the right canvas for grain or motif, while a card or page surface might be.

---



## Phase 1 — Audit current state

Before any token is named, inventory what exists so the system is sized to reality, not guessed.

**1.1 — Dashboard shared components.** Verify [docs/DASHBOARD_UI.md](../../DASHBOARD_UI.md)'s component table (`app-page-header`, `app-data-table`, `app-filter-bar`, `app-active-filters`, `app-column-toggle`, `app-pagination`, `app-empty-state`, `app-stat-card`, `app-toast`, `app-emoji-picker`, `app-person-search-input`) against the actual `shared/components/` tree — flag anything undocumented or removed.

**1.2 — PWA component landscape.** Walk `apps/pwa/src/app/features/` and note which UI patterns are hand-rolled per feature (buttons, cards, modals, form fields) with no shared abstraction — these are the primary migration candidates for the new shared library, since they have no dashboard equivalent to reconcile against.

**1.3 — Drift quantification.** Grep both apps for:

- Raw hex codes in templates/TS (`#[0-9a-fA-F]{3,8}`)
- Arbitrary Tailwind values (`\[#`, `\[\d+px\]`, etc.)
- `.scss` files outside the documented "animations only" exception (rule 3 in [docs/DASHBOARD_UI.md](../../DASHBOARD_UI.md))

   This produces a concrete count — the baseline the Phase 6 guardrail will be measured against (target: zero after rollout, outside the token definition file itself).

**1.4 — Non-DOM color consumers.** Catalog every place color is read outside CSS — `libs/pinyes-render/src/lib/utils/figure-palette.util.ts` is the known one; confirm there are no others (e.g. inline Konva `fill`/`stroke` props elsewhere in `figure-canvas.component.ts`, `tronc-view.component.ts`).

**Deliverable:** a short findings note (can live at the top of `docs/DESIGN_SYSTEM.md` under a "Baseline" heading, or as an appendix here) — not a separate audit doc, to avoid another file to keep in sync.

---



## Phase 2 — Token architecture

The layer everything else is built on. This is where the user's visual identity actually gets encoded — this plan defines the *shape* of that encoding, not its values.

**2.1 — Token categories.**


| Category             | Contents                                                                                                                                                                                                                                                                                                   |
| -------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Color                | `primary`, `secondary`, `accent`, `neutral`, `base-100/200/300`, `base-content` (+ opacity steps already used, e.g. `/60`, `/40`), semantic `success`/`error`/`warning`/`info`, plus whatever the identity needs beyond DaisyUI's default set (e.g. dedicated focus-ring color, elevation-tinted surfaces) |
| Typography           | Font family/families, weight scale, size scale, line-height scale — DaisyUI/Tailwind don't currently carry an opinion beyond "Inter 400–700"                                                                                                                                                               |
| Spacing              | Confirm/extend Tailwind's default scale rather than reinvent it, unless the identity needs a distinct rhythm                                                                                                                                                                                               |
| Radius               | Named scale (e.g. `sm`/`md`/`lg`/`full`) replacing DaisyUI's per-component defaults with one deliberate set                                                                                                                                                                                                |
| Shadow / elevation   | A small named scale (e.g. `raised`/`overlay`/`modal`) instead of ad hoc `shadow-md` sprinkled per component                                                                                                                                                                                                |
| Motion               | Duration and easing tokens (e.g. `fast`/`base`/`slow`, one easing curve) — currently undefined project-wide                                                                                                                                                                                                |
| Z-index              | Only if audit (1.3) turns up ad hoc stacking values worth naming                                                                                                                                                                                                                                           |
| Categorical (canvas) | The qualitative multi-hue set used to distinguish figures/nodes on the Konva canvas — currently hardcoded in `figure-palette.util.ts` — plus canvas-specific surface/shadow colors. Designed as part of the same identity, not a separate palette (see 2.3, 7.2)                                           |


**2.1a — Color mechanism (resolved).** Color splits into a fixed source shared by every colla and a colla-dependent source, combined by one shared state-derivation rule.

*Fixed, from [Origami](https://ggprompts.com/styles/origami.html) — colors only, not that page's type/spacing/radius/shadow/motion (those are separate, still-open categories):*

- Surfaces (`base-100/200/300`): the paper scale — `#FAFAF8` / `#F5F2EC` / `#F8F6F0`, washi `#F0EBE1`/`#E5DFD3`
- Content (`base-content` + its opacity steps): the ink scale — `#1C1B18` → `#B8B4AE`, 5 steps
- Borders/dividers: the crease tones — `#D8D3C8` / `#B8B0A0` / `#8A8070`
- Semantic colors: mapped from Origami's 6 accent hues — `error`←red `#C23B3B`, `success`←green `#3B8C5A`, `warning`←gold `#C9A84C`, `info`←blue `#3B6FC2`. **Fixed and never remapped per colla** — see below. Purple and orange, plus four hues added beyond Origami's original set, become the Categorical (canvas) row's palette — resolved in §2.1i, including why reusing these hues in both places isn't a new confusability risk.

*Why semantic roles never shift per colla:* [CLAUDE.md](../../../CLAUDE.md) already names multi-tenancy as a near-term direction (`ADMIN (≡ TECHNICAL until multi-tenant exists)`) — an admin could plausibly move between collas in one session once that lands. If "error" were red on one colla's theme and orange on another's (e.g. reassigning it away from a colla whose own secondary happens to be red), that breaks a convention every user already carries in from every other app, and breaks it *per tenant*. Rejected in favor of fixing the semantic set and resolving any near-hue coexistence on the fixed side instead (next paragraph).

*Colla-dependent — same derivation shape for both, different fixed L/C target per role, so roles that happen to share a hue never share a value:*

- **Primary** (shirt color): `oklch(L_primary, C_primary, H_shirt)` — hue from the shirt color, lightness/chroma fixed project-wide. Used for buttons and other large interactive fills, so its L/C target favors legibility/contrast at that role.
- **Secondary** (sash color) — two branches:
  - **Real hue** (red, purple, yellow, orange-for-striped, or anything else a sash turns out to be): `oklch(L_secondary, C_secondary, H_sash)` — same mechanism as primary, but `L_secondary`/`C_secondary` are their *own* fixed target, distinct from both `L_primary`/`C_primary` and the fixed `error` token's L/C. Because the two roles have independent (L, C) targets, a full value collision is structurally impossible even when a colla's sash hue lands close to "red" — what remains is a tuning question (next paragraph), not a mechanism gap. Primary defense against confusion is structural either way: error only ever appears via icon + alert/toast/badge components; secondary only ever appears as the striped sash motif — different shapes, different positions, per WCAG 1.4.1 (never convey meaning by color alone).
  - **White or black**: no formula for the *base* — hand-authored presets, because achromatic input has no hue for the formula to consume. White → `paper-white`, dark hairline edge, ink-colored on-secondary content. Black → `ink-black`, light hairline edge, paper-colored on-secondary content.
  - The two-tone diagonal weave texture (every branch, including white/black) is **not** a second hand-picked swatch — it's the same `tone()` primitive described below, applied to whatever the base is. An L-shift doesn't care whether its input is chromatic, so white/black need no special case here. The mandatory hairline edge and the weave tone both hold at any strip thickness (thin or thick), so "thicker strip" doesn't need a different mechanism, just more of the same weave.

*One shared primitive for every "nearby but distinct tone of a base color" need — hover, active, focus, disabled, and the weave companion tone alike:* `tone(base, variant, mode)`, applying a small fixed L delta to any base OKLCH value, delta named by `variant` (`hover`/`active`/`focus`/`disabled`/`weave`), direction flipping between light/dark mode (darker-on-hover on a light surface, lighter-on-hover on a dark one). Applied uniformly to primary, every secondary variant (real-hue *and* white/black), and all four semantic colors — hover/active states for `success`/`error`/`warning`/`info` come from the same function, not separate design work, and the weave pair is just one more named variant of the same call, not a distinct concept. Tints/washes (e.g. a pale success background) use Tailwind's existing opacity-modifier syntax on the base token rather than a dedicated scale. A full multi-stop scale (Tailwind's 50–900 style) is explicitly *not* used here — colla hues are only known at theme-generation time, so hand-tuning a 9-step scale per unknown hue isn't practical, and base-plus-`tone()` covers every case above without it.

*Tuning the error/secondary-red gap:* since collision is structurally impossible (independent L/C targets) but a red-sashed colla could still end up with two reds that feel visually crowded, this gets checked empirically at implementation time — render a red-sash colla's computed secondary next to the fixed error token and see if the gap reads clearly. Origami's literal `#C23B3B` is a *starting point* for error, not a pixel-locked requirement — if the gap needs widening, shift error's hue slightly toward wine/crimson (more magenta-leaning) rather than the more orange-leaning red typical of vivid sports jerseys; still unambiguously "error red," now separated on hue as well as L/C for the one case that needs it.

Exact numeric L/C targets per role (`L_primary`/`C_primary`, `L_secondary`/`C_secondary`, each `tone()` delta) are implementation-time tuning, not decided in this planning doc — the decision locked here is the *shape* of the mechanism, that each role gets its own fixed target so roles never structurally collide, and that semantic roles never remap per colla.

**2.1b — Typography mechanism (resolved).** Two families, role-assigned, both self-hosted: **Fraunces** for display/section-level headings, **Quicksand** for everything else (UI chrome, body, labels, buttons, table cells, card/modal titles).

*Role assignment* — extended one level past the initial display+page-title-only proposal, per feedback that the narrower cut felt thin. The line is drawn at "genuine heading vs. UI-chrome-shaped element," not at a fixed Hn depth:


| Role                                         | Was (DASHBOARD_UI.md)                      | Becomes                                                                                                                                                                                  |
| -------------------------------------------- | ------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Display (hero moments — e.g. PWA login/home) | — (didn't exist)                           | Fraunces 600, tightest line-height                                                                                                                                                       |
| Page title                                   | `text-2xl font-bold`                       | Fraunces 600, tight line-height                                                                                                                                                          |
| Section title (major in-page groupings)      | — (didn't exist as a distinct role)        | Fraunces 400/600, tight line-height                                                                                                                                                      |
| Card/modal title, table header               | `text-base font-semibold`                  | Quicksand 600, normal line-height — stays sans deliberately: these repeat dozens of times per screen in this app, where a display serif would fight legibility rather than add character |
| Label                                        | `text-xs text-base-content/50 font-medium` | Quicksand 500, normal line-height                                                                                                                                                        |
| Body/value                                   | `text-sm`                                  | Quicksand 400, normal line-height                                                                                                                                                        |
| Secondary text                               | `text-sm text-base-content/60`             | Quicksand 400, reduced opacity                                                                                                                                                           |


*Weight scale.* Quicksand: 400 (body), 500 (labels/nav), 600 (emphasis/titles/buttons), 700 (rare strong emphasis). Fraunces: 400 and 600 static cuts (not the full variable range) for the roles above, plus 400/600 *italic* for the case below.

*Line-height.* Three named tokens: tight (display/headings), normal (UI/body), relaxed (long-form prose, if any turns out to exist).

*Italic handling (news module markdown) — confirmed against Google Fonts' CSS API directly, not assumed:* requesting both italic and roman styles for every Quicksand weight returns **only** `font-style: normal` **faces — Quicksand has no true italic anywhere in the family**. The same query against Fraunces returns real `font-style: italic` faces (400 and 600, distinct font files). A browser asked for italic Quicksand therefore falls back to synthetic/faux italic (algorithmic slant) by default.

Given italic use is rare in practice (per the user, outside markdown-rendered news body), the decision is to let the browser synthesize it rather than build something special for a low-frequency case — geometric rounded sans faces generally survive algorithmic slanting better than serifs do. Verify visually once real news content renders; this needs no extra step, it folds into Phase 7's existing real-browser-check requirement. **Held in reserve, not built now:** if synthetic italic looks bad in practice, render markdown `*emphasis`* in Fraunces italic instead of slanting Quicksand — a deliberate family-swap for emphasis, a legitimate editorial technique, not a hack.

*Loading strategy.* Both self-hosted (e.g. `@fontsource/quicksand` + `@fontsource/fraunces`), bundled rather than CDN-linked, loading only the weights listed above. The PWA's offline/service-worker requirements make a runtime Google Fonts CDN request a liability on first load, where a bundled font is covered by the app's existing asset caching for free.

*Deferred, not decided:* Quicksand's legibility at dense table-cell sizes (12–13px) — per explicit direction, kept flexible rather than resolved now; revisit once real screens exist rather than theorizing further.

**2.1c — Domain typography: name legibility (Atkinson Hyperlegible Next).** A third family, scoped narrowly: person names/aliases rendered on figure/tronc nodes need to stay readable from a distance — most acutely in the fullscreen projection view used during live performances, but the same node-label rendering also appears in the editor/assignment canvas. Confirmed available via the same Google Fonts API check used for the other two families: weights 400/500/600/700/800 exist.

*Scope — deliberately narrow, not "canvas text" broadly.* Grepping `figure-canvas.component.ts` for `fontFamily` turns up **18 hardcoded** `'Inter, sans-serif'` **occurrences** — the same file already flagged in the Phase 1 audit (§1.4) for hardcoded colors, now shown to hardcode font too, same root cause, same fix (the Phase 2.3 TS token bridge, since Konva can't read CSS). Not all 18 are person names, though — most render figure/template names in pickers, node-position badges, or help labels, which stay Quicksand like the rest of the app's UI text. The one that matters here is confirmed concretely: `displayText = formatAssignedLabel(assignment.person.alias, node.climbIndicator)`, fed into a `Konva.Text` a few lines later (~line 2363–2390) — the actual label shown on an assigned node. That call (and its counterparts in the canvas's other modes — assignment/segment-assignment/readonly render the same label pattern separately) is what switches to Atkinson Hyperlegible Next; everything else in that file stays Quicksand.

The same rule reaches outside Konva too: `tronc-view.component.ts` (CSS Grid-based tronc rendering, not canvas, per the module's own architecture) and `person-hover-card.component.ts` render `assignment.person.alias` via regular DOM/CSS — no token bridge needed there, just a normal `font-family` reference, but the same font and the same scoping rule (name labels only, not surrounding chrome).

*Weight.* The existing code already toggles `fontStyle: assignment ? 'bold' : 'normal'` at the exact call site found above — recommend mirroring that with 400 (unassigned/placeholder state) and 700 (assigned, the state that most needs distance legibility) rather than introducing new weight logic.

*Loading.* Same self-hosted approach as Quicksand/Fraunces (bundled, not CDN) — this rendering path is reachable from the PWA's offline-capable segment projection view (`SegmentProjectionComponent` → `<lib-pinya-projection>`), so it needs the same asset-caching guarantee the other two fonts already get.

**2.1d — Spacing (resolved).** Reuse, don't reinvent: Tailwind's default 4px-based scale stays as the base unit, unchanged — nothing in the identity defined so far (Origami colors, the three type families) implies a different rhythm, and it's already the documented mechanism (`docs/DASHBOARD_UI.md`'s "Tailwind per layout" rule).

*Spacing vs. sizing — kept separate.* Padding/margin/gap are fully covered by the existing scale; the discipline problem (arbitrary one-off values instead of scale steps) is what Phase 6's guardrail exists to catch, not a gap in the scale itself. Many of the Phase 1.3 "arbitrary Tailwind values" aren't spacing at all — `max-h-[45vh]`, `w-[280px]`, and similar are component *sizing* (widths/heights, viewport-relative dimensions), a related but distinct concern; most of those are legitimate per-component decisions that don't need a shared token.

*Two named-token opportunities, found directly in the Phase 1.3 audit data rather than invented:*

- `max-h-[45vh]` + `w-[280px]` co-occur at **three separate call sites across two files** (`composition-editor.component.html` twice, `distribucio-tab.component.html` once) — a real "compact picker/popover panel" sizing convention that currently exists as three independent copy-pasted magic numbers, not one shared definition. Named as a `panel-compact` sizing token so it's one definition instead of three that merely happen to agree today.
- The documented **24px minimum tap-target** rule (existing accessibility/responsive audit requirement, referenced in `docs/ROADMAP.md`) has no token today — it's enforced by memory/convention only. Formalized as a named minimum-size token so Phase 6's guardrail can check it structurally instead of relying on recall.

*Explicit exclusion.* Node `x`/`y`/`width`/`height` on the Konva canvas (`TRONC`/`BASE` in relative units, `PINYA` in pixels, per `CLAUDE.md`) is domain layout *data* — figure geometry, not design-system spacing, and out of scope here. The canvas's own UI chrome around it (`.canvas-wrapper`/`.zoom-selector` padding in `figure-canvas.component.scss`) is a normal consumer of this scale like any other component — the distinction matters so figure positions never get mistaken for a spacing concern.

**2.1e — Radius (resolved).** Direction: soft/rounded, confirmed.

*Mechanism — reuse DaisyUI's own radius theme keys, don't invent a parallel scale.* Confirmed directly against the installed package (`node_modules/daisyui`, v4.12.24, `src/theming/themeDefaults.js`), not assumed: DaisyUI already ships a small named radius scale as theme variables — `--rounded-box` (cards/modals/larger panels, stock default `1rem`), `--rounded-btn` (buttons/inputs/selects, stock default `0.5rem`), `--rounded-badge` (badges, stock default `1.9rem`), `--tab-radius` (tabs, stock default `0.5rem`). That's exactly the small named scale a "how rounded, and does it vary by component size" decision needs — no reason to add a parallel `sm`/`md`/`lg` system next to one that already exists. Confirmed via `generateCollaTheme` itself that none of these are touched today — it sets colors only — so this slots into the *same* theme object Phase 2.2 already extends, no new mechanism required.

`--rounded-badge`'s stock value is already effectively pill-shaped at typical badge heights — kept that way. A badge's whole visual identity is being a pill; that's not really a "how rounded" question the way box/btn/tab are.

*Why not just keep DaisyUI's stock numbers.* "Looks generic" is the entire complaint driving this project, and a correct *shape* choice (soft/rounded) still reads generic if the exact values are the ones every unmodified DaisyUI app ships with. Recommend picking our own values in the soft/rounded direction rather than silently inheriting the defaults above. Exact rem values are implementation-time tuning, same treatment as Color's L/C targets — the decision locked here is the direction and the reuse-DaisyUI's-4-slot mechanism, not final numbers.

*Canvas bridge — a third instance of the pattern already found for color (§2.1a/2.3) and font (§2.1c).* `figure-canvas.component.ts` hardcodes Konva `cornerRadius` at 7+ separate call sites (values `2`/`4`/`6`, one already named `readonlyCornerRadius`). Same fix: sourced from the Phase 2.3 TS token bridge rather than left as independent magic numbers.

**2.1f — Shadow / elevation (resolved).** Four named steps by role, not by size adjective: `flat` (resting content — no shadow, relies on `base-100`/`base-200` surface contrast alone, consistent with the surface pairing `docs/DASHBOARD_UI.md` already documents), `raised` (cards), `overlay` (dropdowns/popovers/hover cards), `modal` (dialogs — always the deepest).

*Mechanism.* Unlike radius, there's no DaisyUI theme slot to reuse here — confirmed against the installed package's `themeDefaults.js`, no shadow-related keys exist. Implemented instead as CSS custom properties (`--shadow-raised`, `--shadow-overlay`, `--shadow-modal`) alongside the rest of the token layer, each a real `box-shadow` value — optionally multi-layered (2–3 stacked shadows per step, for a more convincing sense of depth than one flat shadow gives) — exact composition is implementation-time tuning, same treatment as every other category's precise numbers.

*Color — warm, sourced from the fixed ink scale, no new hex.* `ink-black` (`#1C1B18`) — the darkest step from Color's fixed ink scale — used as an rgba tint (`rgba(28, 27, 24, alpha)`), alpha increasing per step (lightest for `raised`, heaviest for `modal`). It's genuinely warm already (28,27,24 is noticeably browner than a neutral `#1a1a1a`), so this gets the warm character for free. Referenced as a **literal fixed swatch, not the mode-flipping** `base-content` **token** — deliberately: `base-content` resolves to a *light* value in dark mode (for text contrast), which would turn "shadow" into "glow" if shadows were tied to it. Pinning to the literal ink-black value keeps the tint the same warm color in both modes.

*Dark-mode caveat — flagged now rather than discovered later.* A dark shadow is structurally weak against an already-dark background — not a color choice problem, just physics: you can't get visibly darker than dark. Shadow tokens alone won't carry elevation in dark mode the way they do in light mode. Standard fix (Material's dark theme does the same): pair each elevation step with a slightly *lighter* surface value in dark mode specifically — card a touch lighter than the page, overlay lighter still, modal lightest — so surface-lightening picks up the depth signal where shadow can't. Not fully specified here (downstream of the dark-mode surface values still open from §2.2), just noting the mechanism needs this pairing, shadow alone is insufficient.

**2.1g — Motion (resolved).** A small named duration scale plus one shared easing curve, replacing what's currently ad hoc per file.

*Current state, grounded.* No shared scale exists today — durations found across the codebase's `.scss` files cluster loosely around 0.08s/0.1s/0.12s/0.15s/0.2s/0.25s, each component picking its own number, mixing bare `ease`, `ease-out`, and unspecified defaults for easing. DaisyUI only offers two partial theme slots (`--animation-btn: 0.25s`, `--animation-input: .2s` — durations only, no easing-curve equivalent, so less complete a mechanism than Radius's four slots gave us). There's already good precedent worth keeping, though: `tailwind.config.js` has two deliberately *named* custom keyframe animations, `arrival-bounce` (400ms, the PWA own-position marker's arrival cue) and `restless` (900ms, infinite, the idle nudge hint) — a naming instinct that already exists, just not extended to ordinary transitions.

*Mechanism.* Named duration steps — `fast` (~100–150ms: hover/micro-interactions, matching the existing tight cluster), `base` (~200–250ms: panel expand/collapse, matching the existing looser cluster), `slow` (~350–400ms: bigger deliberate movements — `arrival-bounce` already lives here). Plus **one shared easing curve**, not several, replacing the current mix of bare `ease`/`ease-out`/unspecified — the same "one primitive, reused everywhere" instinct as Color's `tone()` function, giving the motion its own character instead of the browser default. DaisyUI's two existing duration slots get mapped onto this scale rather than left as independent stock values. Exact ms/curve numbers are implementation-time tuning, same treatment as every other category so far.

`restless` stays outside this scale deliberately — it's an ongoing ambient hint, not a response to a state change, a different job than the fast/base/slow set is solving.

*A real gap, not a style question.* Exactly one deliberate `prefers-reduced-motion` check exists anywhere in the codebase — a JS one, in `figure-canvas.component.ts`'s camera-fly animation (`flyToBounds`), done well: jumps straight to the destination, still fires the completion event, only the tween itself is skipped. But there's **zero CSS-level handling** despite 15+ scattered `transition:` declarations, and notably `restless` is an *infinite* loop — exactly the kind of motion accessibility guidance (vestibular-disorder safety) flags as needing to be stoppable, more so than a one-off transition. Part of this category's rollout, not an afterthought: a global rule collapsing all transition/animation durations — and explicitly stopping infinite iteration counts — under `prefers-reduced-motion: reduce`.

**2.1h — Z-index (resolved).** Phase 2.1's table made this conditional on the audit turning up real ad hoc stacking values — a dedicated sweep (broader than Phase 1.3's general drift pass) confirms it did, more thoroughly than first caught.

*What's actually there.* Three different mechanisms doing the same job, uncoordinated: **Tailwind's standard scale** in active use at five different levels (dashboard: `z-50` ×12, `z-10` ×11, `z-40` ×4, `z-20` ×3, `z-30` ×1; PWA: `z-50`, `z-10`); **arbitrary escape-hatch values** — `data-table.component.html`'s `z-[1]` ×3 (sticky headers), and `z-[9999]` appearing **three times, independently**, in dashboard's `toast.component.ts` and PWA's `splash-screen.component.ts`/`toast-container.component.ts` — all three meaning the same thing ("above literally everything else") as three separate magic numbers rather than one; and **raw CSS** `z-index` in `.scss` — `template-editor.component.scss` (10, 30), `figure-canvas.component.scss` (10, the canvas's own zoom-selector/label-editor chrome), `tronc-view.component.scss` (2). Seven-ish distinct numeric levels between three mechanisms, no shared meaning anywhere.

*Mechanism.* A small named scale by role, mapped onto what's already implicitly happening rather than invented from scratch: `raised` (sticky headers, in-canvas chrome — the `z-10`/`z-[1]` cluster), `dropdown` (popovers, tooltips, in-canvas panels — the `z-20`/`z-30` cluster), `chrome` (persistent app header/tab-nav — the `z-40` cluster), `modal` (dialogs — the `z-50` cluster), and a top-of-stack `system` level replacing all three independent `z-[9999]` copies with one shared definition — the same "same intent, multiple silent copies" pattern already found twice elsewhere in this plan (Spacing's `panel-compact` sizing pair, Typography's repeated `text-[10px]`).

**2.1i — Categorical (canvas), resolved.** Two variants, not one — `normal` and `light` — per direction: `normal` for higher-saturation indicator use (rengla markers), `light` for softer/ambient use (projection-mode figure shadows).

*Color set — 10 hues, same order convention as the current* `figure-palette.util.ts`*, values sourced from the fixed theme instead of generic Tailwind defaults.* First six reuse the exact fixed hues already locked in Color (§2.1a) for the semantic roles, in the specified order: red, green, blue, yellow(gold), purple, orange. Extended with four more for headroom, per direction: teal, pink, brown, and — filling the one genuinely empty span of the hue wheel the other nine leave uncovered, between yellow/gold (~50°) and green (~140°), rather than crowding an already-dense neighborhood (a fifth blue-green next to teal/blue, or a fifth red/pink next to purple/pink, would be harder to tell apart at small canvas-node sizes than a hue in open space) — **olive**, proposed as the tenth. Straightforward to swap if a different tenth hue reads better once it's actually on screen.

*Reusing red/green/blue/gold here isn't a new risk — it's the same defense already locked in for secondary/error (§2.1a), applied to a case that fits it even more cleanly.* Semantic colors only ever appear as icon-carrying alerts/toasts/badges in specific chrome positions; categorical colors only ever appear as canvas node fills, rengla indicators, or projection shadows — a completely different visual context, never in the same place at the same time. Same WCAG 1.4.1 shape/position argument, not a new one.

*Light variant — reuse, don't invent, continuing every other category's pattern.* For the six hues shared with the fixed semantic set, Origami already ships hand-tuned light companions for exactly these (confirmed from the original page fetch, not re-derived): `--ori-red-light` `#E8A0A0`, `--ori-green-light` `#A0D4B3`, `--ori-blue-light` `#A0BDE8`, `--ori-gold-light` `#E8D9A0`, `--ori-purple-light` `#C4B0DC`, `--ori-orange-light` `#E8C0A0` — used directly, zero new values needed. For the four hues outside Origami's original set (teal/pink/brown/olive), no pre-authored light companion exists, so these are derived via the same `tone()` primitive already established in §2.1a/§2.1b (one more named variant alongside `hover`/`active`/`focus`/`disabled`/`weave`) rather than a second, disconnected mechanism for "give me a related lighter tone."

*Usage, as directed.* `normal` for rengla indicators (compact, higher-contrast markers). `light` for projection-mode figure shadows — this generalizes what `SINGLE_FIGURE_SHADOW_COLOR` (§1.4/§2.3) was already doing ad hoc with one hardcoded gray regardless of which figure it belonged to: now one light tone per figure's own assigned categorical color, distinguishable figure-to-figure even in shadow form.

**2.2 — Light/dark mechanism.** DaisyUI's `data-theme` attribute already drives runtime theme switching (`document.documentElement.setAttribute('data-theme', ...)`, per [docs/DASHBOARD_UI.md](../../DASHBOARD_UI.md)'s "Theming per Colla" section). Extend `generateCollaTheme` (or replace it with a richer generator) to emit **two** theme blocks per identity — e.g. `<name>-light` / `<name>-dark` — sharing token *names*, differing in *values*. Every component built downstream references the name, never a literal value, so the light/dark swap is free.

*Dark-mode elevation ladder (resolved) — closing a gap left open in §2.1f.* Elevation has four named steps (`flat`/`raised`/`overlay`/`modal`) but the surface scale only has three (`base-100/200/300`), so they can't map 1:1. Resolution: `modal` doesn't need its own surface step — it's always paired with a dimmed backdrop (the near-universal modal pattern), and that backdrop plus the heaviest shadow already carries the "blocking everything else" signal without a fourth distinct lightness. `base-100/200/300` (3 steps, progressively lighter in dark mode) covers `flat`/`raised`/`overlay`'s progression; `modal` rides on `overlay`'s surface plus its own backdrop+shadow, no new token needed.

*Colla-dependent primary/secondary in dark mode (resolved).* One L/C target for both modes, not a separate dark-specific one — consistent with every other exact number in this plan being deferred to implementation-time tuning rather than speculatively building for a problem that might not occur. A colla's computed hue *might* read less legibly on a dark background (a real, known dark-mode color issue), but the fix is to check it in Phase 7's real-browser pass and adjust only if it actually shows up, not to pre-build a second target now.

**2.3 — Reaching non-DOM consumers (the canvas).** CSS custom properties don't reach Konva. **Decided: (a)** — a hand-maintained TS constants module (`libs/ui/src/tokens.ts`) as the single source of truth, with the Tailwind/DaisyUI theme generator *consuming* it (TS → CSS), so there's exactly one place values are typed in, not two that can drift apart — over **(b)**, reading `getComputedStyle(document.documentElement).getPropertyValue('--token-name')` at runtime inside `libs/pinyes-render` setup code. (a) is drift-proof by construction rather than by discipline, and Nx/TS tooling makes "CSS generated from TS" straightforward. Formally closing this out here — every category resolved since (§2.1c, §2.1e, §2.1i) already wrote as though (a) were settled, so this removes a phantom open item rather than leaving it dangling.

   This bridge is what makes the canvas *authorable*, not just technically reachable. Per the full audit (Phase 1, §1.4), it needs to cover four distinct groups currently hardcoded in `figure-canvas.component.ts` and neighbors, not just the palette file:

- `figure-palette.util.ts`'s qualitative figure-distinguishing set (10 hues), `SINGLE_FIGURE_PANEL_COLOR`, `SINGLE_FIGURE_SHADOW_COLOR` — redefined as part of the token module, chosen to cohere with the identity rather than ten arbitrary Tailwind defaults.
- `NODE_COLORS` (zone → color mapping) — domain-driven, but sourced from the token set's values, not independent literals.
- UI-state strokes (`SELECTED_STROKE`, `NORMAL_STROKE`, `CONFLICT_STROKE`, the inline highlighted-state green) — mapped to the same selection/focus/warning/error tokens used everywhere else, not canvas-only constants.
- `ATTENDANCE_COLORS` — rebuilt to read the *same* `success`/`warning`/`error` token values DaisyUI already generates via 2.3(a), rather than a second literal definition that only coincidentally matches today.

   `figure-canvas.component.scss` (DOM chrome around the canvas, not Konva draw calls) needs no bridge at all — it's plain CSS and can reference tokens directly, the same as any other component stylesheet. It already does this *partially* (`oklch(var(--p))` on focus) while hardcoding hex two lines away for hover — the clearest evidence in the whole audit that a token layer without Phase 6's guardrail doesn't self-enforce.

**2.4 — Naming convention (resolved).** Two concrete decisions, now that every category exists to name:

*TS↔CSS mapping.* `libs/ui/src/tokens.ts` uses camelCase (`shadowRaised`), generated CSS custom properties use kebab-case (`--shadow-raised`) — a mechanical, uniform conversion applied everywhere, not a per-file judgment call.

*Prefixing.* New tokens this plan introduced beyond DaisyUI's own vocabulary (the shadow scale, motion durations/easing, z-index roles, the categorical palette, sizing tokens like `panel-compact` and the tap-target minimum) get a shared namespace prefix — `--ds-`* — distinguishing them from DaisyUI's native variables. DaisyUI-native slots this plan is just repurposing (`--rounded-box`, `--rounded-btn`, `base-100`, `primary`, etc.) keep their existing bare names, unprefixed — renaming those would break DaisyUI's own component CSS, which already expects them as-is. The prefix also gives Phase 6's guardrail script an easy, greppable pattern for "was this actually added to the system, or hardcoded."

**Deliverable:** the token set implemented as the new theme generator + (if 2.3a) `libs/ui/src/tokens.ts`, validated by rendering a handful of raw HTML swatches — no app integration yet.

---



## Phase 3 — Shared UI library

**3.1 — New Nx library. ✅ Done.** `libs/ui` created, mirroring how `libs/pinyes-render` is already shared between `apps/dashboard` and `apps/pwa` via `@muixer/pinyes-render`. Import path: `@muixer/ui`. Tagged `["type:lib", "scope:shared"]`, identical to `pinyes-render`; the root `@nx/enforce-module-boundaries` config is already unrestricted (`* → `*), same as `pinyes-render` lives under, so no extra boundary work was needed. Barrel (`src/index.ts`) exports the full Phase 2 token layer and was verified to resolve as `@muixer/ui` from `apps/dashboard` via a real `tsc --noEmit` smoke check, not just within the lib's own tests.

**3.2 — What actually belongs in** `libs/ui`**.** The litmus test is genuine reuse across both apps — now, or clearly imminent — not "is it shared within its own app's `shared/` folder today." A component used by only one app gets restyled with `@muixer/ui` tokens and Tier 1 primitives in place; it does not get relocated just because it happens to already live in that app's own `shared/components/`. Restyling and relocating are different moves, and conflating them was an error in this plan's first draft of this section.

**Tier 1 — Net-new primitives (confirmed dual-app).** Neither app has these as a shared component today; both hand-roll the underlying DaisyUI classes inline. Build these first — highest leverage for personality, since every screen touches them:

1. Button — ✅ done. `lib-button` in `libs/ui/src/lib/components/button/`. `variant` mirrors DaisyUI (minus `outline`, which is its own boolean modifier — real usage always combines it with a color, e.g. `btn-warning btn-outline`, so it isn't a variant of its own), `size`/`shape`/`type`/`ariaLabel`/`disabled`/`loading`/`outline` inputs, `clicked` output, content-projected. Loading auto-disables and shows a `loading loading-spinner` sized to match. Hover is a lift + shadow only (`--ds-btn-lift-shadow`, reusing `SHADOW.overlay`) — deliberately no color change, decided visually after comparing against a color-shift hover across solid/outline/ghost. Press flattens back down with a spring-back release easing and shifts to the role's `--ds-{role}-active` color. Disabled renders as a dashed, unfilled "sketched, not inked yet" outline in the role's `--ds-{role}-disabled` color (the Identity principles' paper metaphor) — required a fix to `tone()`'s disabled shift (`recedeExtremeGap`) since a flat delta made already-light bases like secondary nearly invisible against the surface. Radius reduced project-wide from `0.625rem` to `0.4rem` (`RADIUS.btn`) after seeing the old value read as generic. `conflict` doesn't exist as a variant — that call site becomes `variant="error"`. New theme-level tokens added to support this: `--ds-{role}-hover/-active/-disabled` for all 8 color roles (`InteractiveRole` in `theme.ts`) and `--ds-btn-lift-shadow`.
2. Badge — ✅ done. `lib-badge` in `libs/ui/src/lib/components/badge/`. Much simpler than Button: a static, non-interactive `<span>` — no click output, no loading/disabled, no hover/press personality treatment, since none of the 68 real dashboard usages are interactive. `variant` mirrors DaisyUI (`primary/secondary/accent/neutral/ghost/info/success/warning/error`, default `neutral` — a passive status chip reads better neutral than primary's call-to-action connotation), `size` is `xs/sm/md/lg` (real usage only exercises `xs`/`sm`, but the full scale matches Button's for cross-component consistency), `outline` is the same boolean-modifier pattern as Button's (`badge-outline` combines with a color class in DaisyUI exactly like `btn-outline` does), content-projected (icon+text mixing confirmed by real usage). `badge-conflict` (2 sites) retires to `variant="error"`, same as Button.
  **Custom color support**, needed for domain-assigned colors (tags, figure-node presets — real examples: `tags-list`, `person-detail`, `template-editor`, `attendance-list`). A `color` input takes precedence over `variant` when set: solid fill uses it as the background with content color computed via `contrastContent` (real APCA-based, ink/paper-aware — not `#000`/`#fff`); combined with `outline`, it colors the border/text instead of filling. This is a genuine upgrade over the existing `@muixer/shared`'s `getContrastColor`, which turns out to have the exact same class of bug already fixed in `contrastContent`: a naive relative-luminance formula forcing pure black/white. That utility isn't touched here — its ~10 existing call sites migrate to `<lib-badge [color]="...">` during Phase 7 rollout, not as part of building the primitive.
  **Personality:** explored a deterministic per-badge tilt (rejected — read as gimmicky) and an ink-density gradient fill (skipped — Badge is small/static enough that "no treatment" is the right call here, per the Identity principles' explicit allowance that not every component needs one). Badge ships as a plain flat fill.
  PWA's `pill-badge` turns out not to be the same component despite the name/styling overlap — it's a `<button>` with a mandatory chevron and a `clicked` output (one consumer: profile page's "N delegations →" trigger), not a static label. It doesn't inform Badge's design at all; its one call site becomes `<lib-button variant="ghost">` with the icon/chevron content-projected once Button is rolled out there — a normal rectangular ghost button, not a pill shape (Button has no "pill" shape option, deliberately — the point of a shared shape language is not preserving one-off fully-rounded styling). `pill-badge` itself is deleted, not ported to either component.
3. Card shell — ✅ done. `lib-card` in `libs/ui/src/lib/components/card/`, built on DaisyUI's own `.card`/`bg-base-100`/`shadow-raised` classes (reused as static template classes, not reinvented). `sash: 'none' | 'thin' | 'title'` (default `'none'` — a plain flat card, since not every card needs a treatment, same conclusion as Badge): `'thin'` is a 16px divider band with title/icon in the body below it (replacing the `border-l-4` pattern found 6+ times in the audit); `'title'` is a 38px band carrying the title (and optional icon) directly on the weave. `title`/`icon` (`LucideIconData`) work in all three sash modes. `sashColor`/`iconColor` are optional per-instance overrides (custom hex, content computed via `contrastContent`) — **default is always the shared `--ds-sash-fill`/`--ds-sash-content` token**, never a hardcoded or per-instance color, after an early implementation pass mistakenly varied the sash color per card in a demo grid. `routerLink`/`href`/`clickable` pick the host element (`<a [routerLink]>` / `<a [href]>` / `<button>` / plain `<div>`) via a shared `<ng-template>` outlet, avoiding markup duplication; the interactive variants reuse Button's exact `--ds-ease-spring`/`--ds-motion-base`/`--ds-btn-lift-shadow` tokens at Card's own subtler press scale (`0.98` vs. Button's `0.93`, proportionate to a larger surface) — no color change on hover, matching Button's approved recipe.
  **The sash motif itself** (a real design exploration, not just an implementation detail): the band is a woven two-tone diagonal crosshatch (1px, no seams — several seam/weave-density attempts were rejected as looking too "barber pole"), left edge fades into the weave while overhanging the card's own left edge (so the cloth reads as continuing behind it), right edge is a clean cut where a **fringe** (*flecos*) of wavy, deterministically-generated threads takes over (`sash-fringe.util.ts` — same seeded generator, drop-shadow only; per-thread outline and per-thread shading were both tried against white/black sashes and rejected as looking like a rendering glitch). Radius (`RADIUS.box`) reduced from `0.875rem` to `0.6rem` while designing this — the band's square-cut corners increasingly clashed with a rounder card. A real implementation bug caught before shipping: the band is `position: absolute` (it deliberately overhangs the card's edges) and was not being cleared by `.body`'s padding, so content would have rendered underneath it — fixed with an explicit per-sash-mode `bodyPaddingTop`.
4. Input / form-field — ✅ done (text-like inputs). `lib-input` in `libs/ui/src/lib/components/input/`. Grounded in a 30+-file audit: every hand-written input already shares one exact shape (`form-control` label → optional icon-prefixed `input input-bordered` box → an error/hint line below), but the error line itself had already drifted (`label-text-alt text-error` in most places, a bare `<p class="text-xs text-error mt-1">` in `user-form-modal`) and `label`/`for`/`id` linkage was wired by hand per call site (missing entirely in one, flagged with an eslint-disable). Unlike Button/Badge/Card this is a structural consolidation, not a visual-identity component — it renders through DaisyUI's own `input`/`form-control`/`label` classes with no new tokens, colors, or motion. Implements `ControlValueAccessor` (first use of the pattern in this codebase; every existing form instead binds `formControlName`/`ngModel` straight to a raw native control) so `<lib-input formControlName="email" label="…" [icon]="Mail" />` replaces the whole hand-wrapped block, working with both reactive and template-driven forms. `label`/`hint`/`errorText`/`icon` (`LucideIconData`)/`size` (`xs/sm/md/lg`, default **`sm`** — deliberately deviating from DaisyUI's own `md` default since real usage is 53× `input-sm`/4× `input-xs`/zero `input-md`/`input-lg`)/`type`/`placeholder`/`disabled`/`required`/`autocomplete`/`id` inputs. `hint` and `errorText` share one slot below the input — `errorText` wins when both are set, drives `input-error` + `aria-invalid="true"`, matching the app's existing `field.invalid && field.touched` convention. `id` auto-generates a stable per-instance value when not supplied, wiring `label[for]` and `aria-describedby` automatically — neither was reliable before. `textarea`/`select` deliberately deferred to their own components later rather than folded in here: they don't share Input's icon-prefix-box shape and haven't been audited yet.
5. Modal shell — ✅ done. `lib-modal` in `libs/ui/src/lib/components/modal/`. Audited ~20 modal instances across both apps (not just the four named originally) — every one is CSS-only (`class="modal modal-open"`, toggled by the parent conditionally rendering the component), none call the native `dialog.showModal()`/`.close()`. That's not neutral: `already-assigned-dialog.component.ts` has a hand-written workaround comment explaining `autofocus` doesn't reliably steal focus and Enter keeps hitting the wrong element *because* there's no native focus trap — the same latent bug exists in every other instance, just usually unnoticed. `lib-modal` switches to real native dialog semantics: `open` input drives `showModal()`/`close()` via a constructor `effect()` (reading the `#dialogEl` `viewChild`), giving a native focus trap and top-layer stacking for free. The `closed` output fires from exactly one place — the native `(close)` event — regardless of *how* the dialog closed (Escape → browser's default `cancel`-then-close, backdrop click, the close button, or a parent flipping `open` back to `false`), replacing the three-way-drifted `closed`/`close()`/`cancelled` naming found in the audit. `dismissible` (default `true`) gates both backdrop-click-to-close (checked via `event.target === event.currentTarget` on the dialog, the same technique already used ad hoc in several existing modals) and Escape: the native `cancel` event fires on Escape before close, and `event.preventDefault()` there blocks it entirely when `dismissible` is `false` — exactly the "no backdrop, no Escape, no Cancel, only the accept action" behavior `privacy-consent-modal`/`consent-modal` already hand-implement, now free. `showCloseButton` defaults to `dismissible()`'s own value (on for dismissible modals, off for non-dismissible ones) rather than Card/Button's "always start from a plain default" convention, since the audit showed only 2 of ~20 modals bothered to add one despite most being dismissible — inconsistency, not intent. Setting `showCloseButton` to `true` while `dismissible` is `false` throws in a constructor `effect()` (a hard invariant, not a dev-mode warning like Button's shape/ariaLabel check, since a close button with no working dismissal path is a real contradiction, not a style nit). `size` (`xs/sm/md/lg/2xl` → `max-w-`*, default `md`) matches the real spread found (`sm`×14/`md`×9/`lg`×5/`2xl`×5/`xs`×2) — no `xl`, since no real usage needed it. Body is content-projected directly; footer actions project via `<div modalFooter>` into a `.modal-action` region — both slots stay compositional (no `confirmLabel`/`onConfirm` inputs) since real headers/footers vary too much (title-only vs. title+status badge, one-button vs. two-button footers) to model as config. `modal-box` is bounded to `max-h-[85vh]` with the body scrolling internally (`flex flex-col` + `overflow-y-auto flex-1`) so a tall form or long content (privacy policy text) can't overflow the viewport, which no existing instance guarantees today. Testing note: jsdom (this repo's version) implements `<dialog>`'s `open`-attribute reflection but not `showModal()`/`close()` at all — likely the real reason every existing modal avoided native semantics in the first place, not just convention drift. Added a minimal polyfill to `libs/ui/src/test-setup.ts` (`showModal()` sets the `open` attribute, `close()` clears it and dispatches a `close` event) so `lib-modal`'s tests exercise the real component logic. Also hit a genuine zoneless-testing gotcha while writing the spec: mutating a *host* wrapper component's plain field and calling `detectChanges()` a second time against a signal input throws a spurious `NG0100` in this environment (reproduced with a 15-line component unrelated to Modal) — fixed by testing `ModalComponent` directly via `TestBed.createComponent` + `fixture.componentRef.setInput()` throughout (Button/Badge/Card's existing pattern), reserving a host wrapper only for the one content-projection test, with `open` fixed at `true` from the start rather than toggled.
6. Toast — ✅ done. `ToastService` (`libs/ui/src/lib/services/toast.service.ts`) + `lib-toast-container` (`libs/ui/src/lib/components/toast/`). Audited the two existing systems and found genuine behavioral drift, not just styling: dashboard's had `warning`/`error`/`success`/`info` types, no icons, a flat 4000ms auto-dismiss, and a `remove(id)` method; PWA's had only `success`/`error`/`info` (no `warning`), per-type icons, per-type duration (3000ms success/info, 5000ms error), and a `dismiss(id)` method. Merged deliberately rather than picking one side: kept all four types including `warning` (real call site, shouldn't silently drop it even though used only once), kept PWA's icons (information shouldn't be conveyed by color alone), added an entry animation (`lib-toast-in` keyframe, reusing the shared `--ds-ease-spring`/`--ds-motion-base` tokens rather than PWA's bespoke 0.2s ease-out, for cross-component motion consistency), standardized on `dismiss` as the method name, and settled on a flat 4000ms duration for every type rather than PWA's per-type scheme — simpler and no evidence the per-type split was a deliberate UX call rather than an artifact of two people picking different numbers independently. **Positioning** was the one genuinely deliberate difference (dashboard's corner toast suits a wide desktop viewport; PWA's full-width banner suits a narrow phone screen where a corner toast is cramped) — resolved by making the container responsive by viewport width instead of an app-level `position` input: full-width top banner with `env(safe-area-inset-top)` below the `sm` breakpoint, DaisyUI's own `toast-top toast-end` corner-stacking idiom at `sm` and up. Both apps mount the exact same `<lib-toast-container />` with no configuration; it adapts to actual viewport width rather than hardcoding "dashboard = desktop, PWA = mobile" as a permanent assumption.
8. Checkbox — planned, added mid-Phase-7 (not part of the original Tier 1 list — raised while planning the `segment-workspace` rollout step below, once the actual count made "too simple to bother with a component" not hold up). **25 raw `type="checkbox"` inputs across 17 files**, dashboard + PWA + `pinyes-render` (real `grep` pass, 2026-09-01) — every one hand-wired via `[checked]`/`(change)="...($any($event.target).checked)"`, none of them a real `ControlValueAccessor`, so none work with `formControlName` today. `lib-checkbox` fixes that for all 25 at once (CVA, matching `lib-input`/`lib-select`), and picks up `_fields.scss`'s shared border/focus treatment that already reaches `.input`/`.select`/`.textarea` but not checkboxes today — visibly inconsistent focus ring next to a field that has it. To be **built and every one of the 25 call sites migrated before `segment-workspace` component work starts** (two of the 25 are in its own scope: `figure-properties-panel`'s cordons-oberts toggle, `person-panel`'s Xicalla filter — building it mid-page would mean touching those two twice).
9. Textarea — planned, added alongside Checkbox for the same reason (real count came back much higher than assumed: **10 real files** with a raw `<textarea>|`, dashboard-only — `season-form-modal`, `tag-form-modal`, `attendance-edit-modal`, `event-form-modal`, `person-detail`, `legal-documents`, `notification-send`, `news-editor`, plus two in `segment-workspace` scope: `nodes-tab`'s comodí label dialog, `ad-hoc-node-properties`' decoration label). `lib-textarea` mirrors `lib-input`'s shape exactly (CVA, `lib-form-field` label/hint/error, `maxLength`) — a `<textarea>` in place of `lib-input`'s `<input>`, nothing else new. Same sequencing as Checkbox: **built and all 10 sites migrated before `segment-workspace` starts.**
10. Tabs — ✅ done, added mid-Phase-7 (not part of the original Tier 1 list — raised by the user after noticing the dashboard had grown four independent tab/segmented-control implementations during 7.2's `config` rollout). `lib-tabs` in `libs/ui/src/lib/components/tabs/`. Audit found three genuinely near-duplicate `role="tablist"` implementations (event-detail's Resum/Pinyes/Assistència/Participació, segment-workspace's Pinyes/Troncs/Distribució/Nodes extra/Previsualitza, and the PWA's Propers/Passats filter) plus one that only *looked* like a fourth on sight but wasn't semantically tabs at all: the template editor's Pinya/Rengles/Tronc row was `aria-pressed` toggle buttons, not `role="tab"` — however its underlying signals (`renglaEditMode`/`troncEditMode`, mutually exclusive with the implicit "neither" = Pinya) turned out to be a real three-way exclusive mode after all, so it converted cleanly too, gaining real tab semantics it didn't have before (one behavior change: clicking the already-active tab no longer toggles it back off, since tabs don't self-deselect — judged correct, not a regression). The top bar's Inici/Persones/Assajos nav (`tab-nav.component`) was deliberately excluded after discussion with the user: it's route-driven with a mobile hamburger-collapse fallback (a different responsibility — app-level navigation, not in-page section switching) and stays visually distinct (underline, shirt-colored) on purpose. `lib-tabs` takes a data array (`TabDef[]`, `{id, label, icon?}`) + `activeId`/`(activeIdChange)` — deliberately "dumb" like `lib-button-group`: it owns the `tablist` nav and ARIA wiring only, not content switching, since the two dashboard call sites differ there (eager `@switch` vs. lazy-mount-then-keep-alive `hasVisited`) and forcing one scheme into the primitive would have meant picking a side with no real evidence either was wrong. Internalizes the WAI-ARIA roving-tabindex keyboard pattern (Left/Right/Home/End) that only the PWA had before — every consumer gains it uniformly. `style: 'boxed'|'bordered'` maps 1:1 to DaisyUI's own modifiers (event-detail/segment-workspace/template-editor all default to `boxed`; the PWA's `bordered` usage was left untouched, per explicit user instruction to keep the PWA out of scope this round). Two small opt-in inputs (`testIdPrefix`, `panelIdPrefix`) exist purely to keep event-detail's pre-existing `data-testid`/`aria-controls` contract (its spec already queried by those) working unchanged through the swap, not because every consumer needs them. Icon type reconciled to `LucideIconData` objects (matching `lib-card`/`lib-badge`'s existing convention) — event-detail and the template editor previously passed either bare string icon names (`'Info'`, `ICON_FIGURA`-the-string-constant) or `DOMAIN_ICONS.X`-the-object depending on the file, an inconsistency this forced into the open and fixed at both sites. One `display:contents`-vs-margin-utility bug shipped and was caught the same day: `event-detail`'s `space-y-4` outer container had `<lib-tabs>` as a direct child, silently losing the gap above the tab strip — fixed with a `<div class="mt-4">` wrapper (see `docs/DESIGN_SYSTEM.md`'s `lib-tabs` entry for the general pattern). Verify: `nx lint`/`build`/`test ui`+`dashboard` all clean — `lib-tabs` itself has 10 passing unit tests, and the only dashboard test failures throughout were the same 4 pre-existing unrelated `person-detail` ones. Two `segment-workspace.component.spec.ts` assertions that inspected the old raw DOM structure directly (`nav.nextElementSibling`) were updated to check the equivalent classes without relying on sibling adjacency, since `lib-tabs`'s `display:contents` host changes the DOM nesting one level. This work landed ahead of the Phase 7 rollout's own page order — it touches `events` (row 7) and `pinyes/segment-workspace` + `pinyes/template-editor` (row 8) before either row's own turn — so those rows' future full-page passes should treat their tab strips as already done, not re-touch them.



**Decided while designing Button: retire the** `conflict` **color entirely, use** `error` **instead — ✅ fully done as of the `pinyes-tab`/`segment-workspace` pass.** `tailwind.config.ts` defined a standalone custom color `conflict: #e11d48` (dashboard's `styles.scss` also hand-declared `--conflict: #e11d48`), used across `btn-conflict`/`badge-conflict`/`alert-conflict`/`text-conflict`/`border-conflict`/`bg-conflict`/`border-conflict`. It was a second, unrelated red sitting next to the theme's actual `error` semantic token (`#C23B3B`) for the same "something's wrong" meaning — the two-reds-for-one-meaning problem, not a deliberate distinction. `event-participation`/`segment-manager` migrated to `variant="error"`/`text-error` in an earlier pass (see `events` row above); the remaining call sites — `segment-conflict-panel` (raw markup, its own Tier1/2 pass still pending — only the color fixed here: `bg-error`/`border-error`/`text-error`/`btn-error`/`badge-error`), `already-assigned-dialog` (now `lib-badge variant="error"` for the TRONC placement chip, `lib-button variant="error" outline` for "Assignar igualment", `text-error` for the tronc warning line), and `import-pinya-modal` (`alert-error`, still raw — no `lib-alert` primitive exists yet, unrelated to the color question) — all converted in the same pass once the user flagged that the earlier "flag it as a gap" call had missed this already-decided plan. The custom `conflict` Tailwind color and the SCSS var/classes are now deleted (`tailwind.config.ts`, `apps/dashboard/src/styles.scss`) — confirmed via a full-repo grep that no consumer referenced them anymore before deleting, and via the compiled dashboard CSS that no `conflict` class survives. `CONFLICT_STROKE` in `figure-canvas.component.ts` (Tier 5, canvas) is untouched — it's a plain hardcoded hex constant, not a CSS-var read, so it wasn't broken by the SCSS var's removal, and its own migration to the `error` token stays Tier 5's job as originally planned.

**Tier 2 — Reconciliation target (confirmed dual-app).** The one component both apps already share by name, with drifted APIs:
   7. Empty-state — ✅ done. `lib-empty-state` in `libs/ui/src/lib/components/empty-state/`. Resolved each drift point rather than picking a side wholesale: no card wrapper (PWA's bare layout — it sits inside whatever container the consumer already provides, matching Card/Badge's "don't add chrome nobody asked for" precedent); `icon` is a typed `LucideIconData` input (PWA's convention, already the cross-cutting rule in §3.3); output renamed to `clicked`, matching Button/Card's existing naming rather than either side's `actionClick`/`action`. Also fixed a real bug found while re-auditing, not just a style difference: PWA's `icon` had a hardcoded `Calendar` default, so call sites that never override it (`profile.component.html`'s "No s'ha pogut carregar el perfil", `event-list`'s "S'ha produït un error en carregar...") were rendering a calendar icon on a profile-load failure and a generic error — wrong icon, silently. `icon` has no default now (dashboard's already-correct "optional, no icon shown if unset" behavior); `role="status"` kept from PWA's version (dashboard's had neither that nor any live-region semantics).

**Tier 3 — Candidates evaluated and judged single-app.** Restyled with `@muixer/ui` tokens/primitives in place; stay in their current app. **Not a standalone one-by-one pass like Tier 1/2** — unlike a net-new primitive, there's no API to design here, just an existing single-app component swapping its hand-rolled DaisyUI classes for the library's tokens/primitives. That naturally happens *when its page is touched* during Phase 7's page-by-page rollout (7.2/7.3), not as a separate tier worked through in isolation beforehand or afterward — Tier 1/2's primitives need to prove themselves against real usage first (Phase 7.1) regardless, and restyling a Tier 3 component the same day its page gets migrated avoids touching it twice:

- `apps/dashboard` (desktop admin-list UX with no PWA analogue): `data-table`, `filter-bar`, `active-filters`, `column-toggle`, `pagination`, `tab-nav`, `header`, `page-header`, `color-picker`, `emoji-picker`, `person-search-input`, `tutorial-modal`, `user-chip`, `stat-card`.
- `apps/pwa` (mobile-shell/gesture patterns dashboard will never need): `bottom-tab-bar`, `mobile-header`, `no-person-banner`, `person-switcher`, `pull-to-refresh`, `splash-screen`, `person-data-fields`, `skeleton-card`.
- Not ported at all: PWA's `pill-badge` is deleted once Tier 1's badge exists.
- Deliberately excluded from this list: dashboard's `controls/node-actions`/`controls/node-dpad` are pinyes-canvas-specific (figure-assignment controls), not generic UI — closer to `libs/pinyes-render`'s territory than `libs/ui`'s, if they move anywhere at all.

**Tier 4 — Open, real dual-app candidates.** None remaining — the last open candidate (`skeleton-card`) was resolved single-app, see Tier 3.

**Tier 5 — Canvas token bridge (not a DOM component).** The Konva canvas (`libs/pinyes-render`) is explicitly in scope (see the Problem section, §1.4, §2.3) but doesn't fit the Tier 1–4 shape — it's not a component with `variant`/`size` inputs, it's TypeScript draw calls that need literal resolved values. §2.3 already decided the *mechanism* (a TS-first token source, which Phase 2 already built: `libs/ui`'s tokens are plain TS functions/constants, and `tailwind.config.ts` consumes them — not the reverse). What's not yet done is actually pointing `libs/pinyes-render` at them. Per the §1.4 audit, this covers:

- `figure-palette.util.ts` (12 hex) — the qualitative figure-distinguishing palette; replace with `buildCategoricalPalette()` from `@muixer/ui`.
- `figure-canvas.component.ts` (64 hex + 18 hardcoded `'Inter, sans-serif'`) — grid/node/stroke colors and the assigned-label font; replace with resolved token values (colors) and Atkinson Hyperlegible Next for person-name labels specifically, Quicksand elsewhere, per §2.1's font-scoping rule.
- `figure-canvas.component.scss` (8) — this is DOM chrome around the canvas (wrapper, zoom selector, label editor, fit button), not Konva draw calls, so it restyles through ordinary Tailwind/DaisyUI classes like any other component — no bridge needed here.
- `tronc-view.component.ts`/`.scss`, `own-position-marker.component.html`, `person-hover-card.component.ts` (14) — scattered UI-state colors in the surrounding rendering components; same TS-bridge treatment as `figure-canvas.component.ts` where they're Konva/canvas-side, ordinary Tailwind where they're DOM-side.

   Open question, not resolved here: since `@muixer/ui`'s token functions already return `OklchColor`/formatted `oklch(...)` strings, Konva may be able to consume those directly as `fill`/`stroke` values (modern Canvas 2D `fillStyle` parsers accept `oklch()` in current browser versions) — worth a real browser check before assuming a hex-conversion step is needed. Scheduling: per §7.2, the actual restyle happens during the `pinyes` rollout step (Phase 7, last in the dashboard sequence, since it's the most complex and is shared with the PWA's projection view) — this Tier 5 entry exists so the work is tracked as part of the component inventory, not just implied by "pinyes is last."

**3.3 — Component conventions.** Standalone + `OnPush` + Signals (`input()`/`output()`), per [CLAUDE.md](../../../CLAUDE.md)'s existing Angular rules — no change to those, just a new home for the Tier 1/2/4 components that do move. Every component styled exclusively through tokens/DaisyUI classes — no component-local hex, no component-local one-off spacing. Three cross-cutting conventions, settled before Tier 1 starts so they don't drift per-component:

- **Selector prefix:** `lib-`**.** Both apps' own components use Angular CLI's default `app-`; `pinyes-render` already established the precedent that `app-` is reserved for components moved *verbatim* from an app (selector kept to avoid breaking call sites), while `lib-` is for anything authored fresh inside the library. Every Tier 1/2 component is net-new, so all of `libs/ui` uses `lib-` (`libs/ui/eslint.config.mjs` updated accordingly).
- **Variant/size vocabulary mirrors DaisyUI directly** (`primary`/`secondary`/`accent`/`neutral`/`ghost`/`outline`, `xs`/`sm`/`md`/`lg`, …) rather than a custom parallel naming scheme — no translation layer to maintain, stays legible to anyone who already knows DaisyUI, and DaisyUI is a load-bearing foundation here per CLAUDE.md, not an implementation detail worth hiding behind an abstraction.
- **Icons:** `LucideIconData` **typed inputs**, not string icon names. 70 files across both apps already use `lucide-angular`; the two existing `empty-state` implementations disagree on exactly this point (dashboard: string name, PWA: typed `LucideIconData`) — one instance of the Phase 1 API-drift finding. Standardize on PWA's pattern (more type-safe, already the majority convention).



   Left deliberately undecided in the abstract — the hover/press motion recipe, the focus-ring treatment, disabled styling — these get settled concretely while building Button (the first component) and reused as precedent from there, rather than speculatively decided before any component exists.

   **Shared press/release motion, promoted to tokens while designing Card.** Button's press-release spring was initially a literal `cubic-bezier(0.34, 1.56, 0.64, 1)` hardcoded in `button.component.scss` — fine in isolation, but Card needed the identical curve (at its own, subtler press-scale) and would otherwise have duplicated the literal independently, with no single place to retune "springier" or "faster" project-wide. Promoted to `EASE_SPRING` in `motion.ts`, exposed as `--ds-ease-spring` / `--ds-motion-base` (220ms, `DURATION.base`) in `theme.ts`, same pattern as `--ds-btn-lift-shadow`. Button retrofitted to consume the CSS variables instead of the literal. The convention going forward: shared *timing* (duration, easing) always becomes a token, since it's a project-wide identity trait; press *scale magnitude* stays a per-component constant (Button: 0.93, a small control; Card: 0.98, a large surface) since the right amount of movement is legitimately size-relative, not one global number.

**Deliverable:** `libs/ui` exists, builds, is imported by both apps for at least the Tier 1 primitive batch.

---



## Phase 4 — Documentation

**4.1 —** `docs/DESIGN_SYSTEM.md` becomes the canonical source of truth. Structure:

- Principles — carries over the "Identity principles" section near the top of this doc, expanded with concrete examples from the finished component set
- Token reference tables: color, typography, spacing, radius, shadow, motion (values + usage guidance, mirroring the table style already used in [docs/DASHBOARD_UI.md](../../DASHBOARD_UI.md)'s "Paleta de Colors" section)
- Component inventory with links to the Phase 5 live reference
- Usage rules (do/don't), in the same terse rule-list style as [docs/DASHBOARD_UI.md](../../DASHBOARD_UI.md)'s "Regles d'Estil"
- Theming/dark-mode instructions (supersedes "Theming per Colla")
- Accessibility notes (contrast minimums the token values must satisfy)

**4.2 —** `docs/DASHBOARD_UI.md` narrows to dashboard-*specific* content only (layout shell, routing table, page composition patterns) and drops the now-duplicated "Paleta de Colors"/"Tipografia" sections in favor of a link to `DESIGN_SYSTEM.md`.

**4.3 — PWA UI doc.** The PWA currently has no equivalent to `DASHBOARD_UI.md`. Open question below: whether it gets its own thin layout/routing doc mirroring the dashboard's, once it has enough shared-library-driven structure to document.

**4.4 — Map maintenance.** Per [CLAUDE.md](../../../CLAUDE.md)'s documentation rules: add `docs/DESIGN_SYSTEM.md` to [docs/MAP.md](../../MAP.md)'s table and mermaid graph, give it frontmatter `tags: [domini]` (or `qa`, matching `DASHBOARD_UI.md`'s precedent) and a `Veïns:` footer, then run `pnpm run docs:map`.

---



## Phase 5 — Living style guide

A doc can go stale silently; a rendered page can't lie about whether a component actually looks like its spec.

**5.1 — Route.** A dedicated route (e.g. `/design-system`) inside the dashboard app — it already has more chrome/screen real estate and tab navigation to host it. Behind the existing `authGuard` + `rolesGuard(TECHNICAL, ADMIN)` (no need for a public route; this is an internal tool).

**5.2 — Contents.** Renders, with real (not placeholder) content:

- Every color token as a swatch, with a light/dark toggle
- Type scale samples at each defined size/weight
- Spacing/radius/shadow/motion samples
- Every `libs/ui` component, every documented variant and state (default/hover/focus/disabled/error)

**5.3 — Why not Storybook (for now) — resolved.** Given the existing Nx/Angular setup and that both consuming apps already exist, an in-app route is faster to stand up and keeps the reference inside the same auth/theme context it's describing. Decided: no Storybook for this rollout. Not ruled out permanently — it remains an option later if isolated component development turns out to need it — but not part of this plan.

**5.4 — PWA visibility.** Since components are physically the same (`libs/ui`), the dashboard route is sufficient as the canonical viewer — no need to duplicate the route in the PWA.

**✅ done.** `/design-system` ships as a lazy dashboard route, reachable via a card on `/config` — restricted to **ADMIN only** (tighter than the 5.1 draft's TECHNICAL+ADMIN, since this is a build tool, not a real end-user feature), guarded both at the route (`rolesGuard(UserRole.ADMIN)`) and the `/config` card (`@if (auth.isAdmin())`). Four section components (`color-section`, `typography-section`, `tokens-section`, `components-section`) render every token and all 7 shipped components with real values — no placeholders: semantic/surface/interactive-state/categorical color swatches read the live theme (a light/dark toggle flips `document.documentElement`'s `data-theme` between the two registered colla themes and restores the prior value on destroy — the one piece of real logic, TDD'd); type family samples note there's no separate custom type-size scale (Tailwind's stock one is used as-is); radius/shadow/motion/z-index render every named token; the component gallery demos every documented variant and explicit state (disabled/loading/error/dismissible vs. non-dismissible modal/all 4 toast types), with hover/focus left live on the real controls rather than faked.

Two real bugs surfaced and fixed while building this, both on the critical path for the page being honest: (1) both apps' `index.html` had `data-theme="colla-barcelona"`, which matches neither registered DaisyUI theme (`colla-barcelona-light`/`-dark`) — neither app was ever actually applying a real theme at all; fixed to `colla-barcelona-light`. (2) `lib-toast-container` renders `CheckCircle` for success toasts, but neither app's global Lucide icon provider had registered it — first real mount of the component (this page) would have shown a blank icon; added to the dashboard's `app.config.ts`. Also fixed a barrel gap: `typography.ts` wasn't exported from `libs/ui`'s `index.ts` at all.

Separately, a Button bug the user caught while reviewing the page: outline buttons rendered borderless (DaisyUI's own `.btn-outline { border-color: currentColor }` was being clobbered by this project's `.btn { border-color: transparent }`, which unconditionally out-specificities it once Angular's encapsulation attribute is added — fixed by scoping to `:not(.btn-outline)`), and hover changed fill/border/text color on both solid and outline buttons despite the SCSS comment claiming otherwise — DaisyUI's own hover rules were never actually neutralized, just asserted-in-a-comment to be. Fixed with per-case overrides (generic `--btn-color`-based reassertion for solid, forced-transparent for ghost, per-role reassertion of DaisyUI's own resting formula for outline) each deliberately built to outrank the DaisyUI rule they replace by specificity, not source order.

---



## Phase 6 — Guardrails against drift

Documentation and a style guide describe the system; guardrails keep code from disagreeing with it.

**6.1 — Static check script.** A CI-run script (same spirit as `pnpm run lint:dead` / knip) that fails the build on:

- Raw hex codes outside the token definition file(s)
- Tailwind arbitrary-value syntax (`bg-[...]`, `text-[...]`, etc.) outside justified exceptions

   This directly extends the existing documented rule "Mai classes Tailwind dinàmiques" in [docs/DASHBOARD_UI.md](../../DASHBOARD_UI.md) — same category of rule, now checked instead of trusted.

**✅ done, warn-only.** `scripts/check-design-tokens.mjs` (`pnpm run lint:tokens`) walks `apps/dashboard/src`, `apps/pwa/src`, `libs/pinyes-render/src`, `libs/ui/src` (excluding `libs/ui/src/lib/tokens/` itself) for raw hex literals and color-related Tailwind arbitrary values (`bg-[...]`, `text-[...]`, etc. — deliberately scoped to color-relevant prefixes only, not every arbitrary value like the already-justified `max-h-[85vh]` on `lib-modal`), and reports file/line findings. Wired into `.github/workflows/ci.yml` as its own step, always exiting 0 — **warn-only was an explicit user decision**, not a default: Tier 3 restyling (~25 components) and Tier 5 (the Konva canvas) haven't happened yet, so a hard fail today would block unrelated PRs on pre-existing drift rather than catching new regressions. First real run found 110 findings across 21 files — almost entirely the already-known Tier 3/Tier 5 backlog (`figure-canvas.component.ts` alone accounts for ~70, all documented as Tier 5). Ratchet to a real failing CI step once that drift clears (Phase 7).

**6.2 — CLAUDE.md update.** Add a "Design system" line to the Frontend dashboard / PWA sections of [CLAUDE.md](../../../CLAUDE.md) pointing at `libs/ui` + `docs/DESIGN_SYSTEM.md`, so any future agent session (including future me) is structurally pointed at the shared library instead of free-handing new UI.

**✅ done.** Both sections updated. Dashboard section also got two accuracy fixes noticed in passing: the route list was missing `/config/legal` and the new `/design-system`, and the `Theme generated via generateCollaTheme(primaryHex) in tailwind.config.js` line was stale on two counts (the function takes `shirtHex, sashSpec` since Fase 5's sash work, and it's defined in `libs/ui/.../theme.ts`, only *imported* by `tailwind.config.ts`).

**6.3 — Visual regression net.** The existing Playwright audit suite ([docs/AUDIT_SUITE.md](../../AUDIT_SUITE.md)) is the natural home for snapshot tests against the Phase 5 style-guide route — catches accidental token/component changes going forward. Scope/depth of this is an implementation-time decision, not blocking the plan.

**✅ done, scoped narrow.** `apps/dashboard-e2e/src/audit/design-system-audit.spec.ts`, same idiom as every other `*-audit.spec.ts` (there's no pixel-diff baseline tooling anywhere in this repo — these are full-page screenshots for manual review plus the standard overflow/tap-target/console-error checks, not automated visual diffing). One route, all 4 device profiles via the existing project matrix. Confirmed discoverable via `playwright test --list` (not run end-to-end this session — needs a live API+Postgres+ADMIN credentials per [docs/AUDIT_SUITE.md](../../AUDIT_SUITE.md), same as every other audit spec). Does not capture the dark-mode toggle state (no per-route post-navigation hook exists in `audit-core.ts`'s `RouteDef` — would need a bespoke step outside the shared loop); left as a future refinement, not blocking.

---



## Phase 7 — Rollout

Matches the "foundation first, then both apps" sequencing already agreed:

**7.1 — Sub-phase A: Foundation.** Phases 2–6 above, validated end-to-end against 2–3 representative screens picked from *both* apps — not a full app migration yet, just proof the pipeline (token → component → doc → style guide → guardrail) works. Real browser check required before calling this done, per [CLAUDE.md](../../../CLAUDE.md)'s UI-change testing rule.

- **Toast is a one-shot global swap, done here, not page by page.** Unlike Button/Card/Input/Modal/EmptyState (swapped incrementally per page below), there's exactly one mount point per app — `app.html`'s `<app-toast />` — and one `ToastService` every feature injects. Do the swap once: mount `<lib-toast-container />` in both apps' shells, redirect every injection site from the dashboard's `shared/components/feedback/toast/toast.service.ts` (and the PWA's equivalent) to `@muixer/ui`'s `ToastService`, delete the old component/service pair once nothing references it. If this waits until some later page's turn, every page migrated *before* that turn would still be calling the old service — so it has to happen in the foundation step, not mid-sequence.
- **Representative screens:** `login` (auth, smallest real surface, exists in both apps, first thing every user sees) + `home` (both apps) + `persons` list (dashboard only — the smallest of the three `app-data-table` consumers, see 7.2's table below) — covers a form-heavy page, a card-tile page, and a data-table page across both apps before committing to the full sequence.

**7.2 — Sub-phase B: Dashboard rollout.** Page-by-page, ascending complexity — order below is grounded in a real `grep` pass (raw `.btn`/`.card`/`.badge`/`<input>`/modal-file counts per feature directory, 2026-08-18), not a guess:


#### 1. `/home`

**Rough size:** 3 buttons, 20 `.card`-classed elements, 1 empty-state

✅ done — Lightest page. 

Nav-tile/quick-link cards removed per user request rather than converted (info now lives in the two remaining summary cards, made clickable via `lib-card routerLink`). Two real `lib-card`/`lib-button` API gaps found and added here: `routerLink`/`href` link mode on `lib-button`, `tone` on `lib-card`. `font-serif` precedent set on the greeting `<h1>`.

#### 2. `auth` (login, forgot/reset password)

**Rough size:** 3 files, 3 buttons, 5 inputs

✅ done — login was *not* actually covered in 7.1 despite the note below (toast + representative-screen check happened, login's own component swap didn't) — done here instead, alongside forgot/reset password. Hardcoded `shadow-lg` on the floating auth card mapped to the `shadow-modal` token (closer semantic fit than `lib-card`'s hardcoded `shadow-raised` — considered and rejected `lib-modal` too: a full-page destination with nothing behind it and no dismiss action isn't a modal). Two real gaps found and added: `min-h-6` tap-target baked into `lib-input`'s native `<input>` (WI-03 parity, previously only patched ad hoc on these exact pages) and a `fullWidth` input on `lib-button` (a `class="w-full"` on the tag is inert — `display: contents` — and was only *appearing* to work here via the parent flex column's default item-stretch; confirmed every other raw `btn...w-full` instance in the app, ~10 files, relies on a real class on a real box instead, so this needed a real component input, not a coincidence). Two more found on user review of the rendered page: `lib-input`'s default `size` (`sm`, 32px) silently shrank fields that used to be DaisyUI's implicit `md` (48px, no size class in the old raw markup) — no library change needed, `size="md"` already existed as an input, just had to be passed explicitly (now called out in `lib-input`'s doc row so the other ~10 files with the same raw-markup shape don't repeat it blind); and `loading` visually read as `disabled` (dashed "sketched" outline / DaisyUI's own grey-out) since it reuses the native `disabled` attribute for real double-submit prevention — fixed with a `lib-btn-loading` marker class whose CSS reasserts the exact resting-state fill/border/text formula (solid and outline alike) while `:disabled`, so loading now reads as *busy*, not *disabled*, for every `lib-button` in the app, not just this page.

#### 3. `sync` + per-module `*-sync` components (`persons/person-sync`, `events/event-sync`)

**Rough size:** ~4 buttons each, no modals

✅ done — Small and mechanical, no modal-rewrite risk — a low-stakes place to build page-by-page rhythm before the bigger features. 

The 3 sync-target cards on the global hub iterated live with the user: tone-tinted `lib-card` (role-colored background) rejected in favor of plain cards with `sash="title"` bands (matching `/home`'s own two feature cards), then the per-card CTA buttons dropped entirely in favor of making the whole card clickable via `lib-card`'s own `routerLink` — all three now uniform `variant="primary"` rather than color-coded per target. Caught two real pre-existing bugs, neither introduced by the migration: (1) the sync hub's `/persons/sync` link never matched any route (`persons.routes.ts` names it `sync-start`, unlike `events.routes.ts`'s consistent `sync` for both rehearsals/performances) — silently fell through to the `:id` route and 404'd as "Persona no trobada"; (2) `person-sync`/`event-sync`'s outer container used `space-y-4` with `lib-card` as a direct child — margin-based spacing has no effect on a `display:contents` host, so there was no gap above the first card (same class of bug as auth's `w-full`, different property). Also re-pointed both sub-pages' back buttons to `/sync` (the hub) instead of their own list, per user request. `lib-card`'s `icon` input needs actual `LucideIconData` objects (`DOMAIN_ICONS.X`), not the string icon-name constants (`ICON_X`) used for raw `<lucide-icon name="...">` — an easy mix-up since both live in the same `domain-icons.ts` file. 

**Follow-up, user-flagged:** the per-page checklist's typography step (#4) was skipped on the first pass — none of the three real page `<h1>`s got `font-serif`, and the `alert-warning shadow-sm` on all three pages was an untokenized DaisyUI default masquerading as fine (checklist step #3) rather than `shadow-raised`. Both fixed after the fact; `sash="title"` card titles correctly stayed sans (Quicksand), since `DESIGN_SYSTEM.md`'s typography table marks card/modal titles as sans-only.

#### 4. `communication` (news list + editor)

**Rough size:** 8 buttons, 1 modal, 1 empty-state

✅ done — First real `lib-modal` usage in production (the design-system reference page was the only prior instance). 

The delete-confirm modal now uses `[open]="!!confirmDeleteTarget()"` + `(closed)="cancelDelete()"` (always rendered, never conditionally mounted, matching the reference page's own pattern) rather than wrapping it in an `@if...as`; `[loading]="deleting()"` on `lib-button` replaced the old manual `[disabled]` + hand-rolled spinner span entirely. The delete button (both the modal's and the row action's) uses `variant="error" outline` — literally `DESIGN_SYSTEM.md`'s own documented canonical example for a destructive action, not an ad hoc choice. `app-empty-state`/dashboard-local swapped for `lib-empty-state`; needed two new `DOMAIN_ICONS` entries (`NOTICIA`, `BELL`) since `lib-empty-state`/`lib-card`'s `icon` input takes `LucideIconData` objects, not the string constants (`ICON_NOTICIA`) used for raw `<lucide-icon name="...">`. The communication hub got the same `sash="title"` + whole-card `routerLink` treatment as the sync hub (#3); its disabled "Notificacions" (coming-soon) card has no card-level disabled state to reach for, so it's a plain `lib-card` wrapped in an external `opacity-60 cursor-not-allowed` div instead — that class can't go on `lib-card` itself (`display:contents` host, same class of bug as everywhere else). The news editor's title `<input>` became `lib-input`; the body `<textarea>` and the `datetime-local` input + its `join`-grouped "Ara" shortcut button stayed raw DaisyUI — no `lib-textarea` exists yet, and `InputType` doesn't include `datetime-local`, so forcing either through the library would've been a fabricated fit, not a real one. Kept the "Ara" button raw for a second reason too: its test queries `[data-testid="publish-now-button"]` and calls `.click()` on the match — on a `lib-button` that attribute would land on the inert `display:contents` host (found, not hit — same class of bug as `formcontrolname` in 7.2's login page), and clicking the host doesn't bubble down to the real inner `<button>`'s listener, so the test would silently stop exercising the click handler. Two of the two `space-y-4` wrapper divs newly holding a `lib-*` component as a direct child were caught before shipping (not after, this time) and switched to `flex flex-col gap-{n}` up front. 

**Follow-up, user-flagged, same miss as #3:** typography (checklist step #4) skipped on the first pass for all three real `<h1>`s here too (hub, editor, and — since `app-news-list` was the first page in this rollout to touch the shared `app-page-header`, per checklist step #2 — its `<h1>` as well, rippling to every other not-yet-migrated page that reuses it); fixed together with #3's. Also caught while fixing: the markdown-help tooltip's `shadow-lg` was the same untokenized-default issue as #3's `alert-warning shadow-sm`, mapped to `shadow-overlay` (a hover-triggered floating popover, not a resting card) rather than `shadow-raised`. 

**Follow-up, user-requested content pass:** the hub's "Notícies" card felt empty on a real browser check — swapped its static description for live data (`NewsService.getAll()`, TDD'd in `communication.component.spec.ts`). Iterated with the user to its final shape: a `recentPublished` computed (newest-first, capped at 3) and an `upcomingScheduled` computed (soonest-first, capped at 3) rendered as two labeled title+date lists sharing one `<ng-template>`/`ngTemplateOutlet` (avoids duplicating the `<li>` markup — same technique Button/Card already use internally), plus a plain-text `newsCounts` line ("N publicades · N programades · N esborranys") placed *after* the headlines, not before — `lib-badge`s were tried first for the counts but rejected by the user since the exact numbers aren't the important information, headlines are. Also added a "Notícia nova" `lib-button` next to the hub's `<h1>`, routed to `news/new` — deliberately placed outside `lib-card`'s own clickable `routerLink` wrapper, since nesting a second link/button inside it would create an invalid/broken nested-anchor (the card already routes the whole tile to `news`). The "Notificacions" placeholder card was deliberately left static — the user's call: it'll get the same real-data treatment once that feature ships, not before.

#### 5. `persons`

**Rough size:** list: 6 buttons, `app-data-table`, 1 empty-state · detail: 16 buttons (biggest single file outside `events`/`pinyes`), 2 modals (`person-new-modal`, `person-delegate-modal`)

✅ done — Biggest single item so far. 

`person-list` is the first page in this rollout to touch `app-data-table`/`app-filter-bar`/`app-active-filters`/`app-column-toggle`/`app-pagination`/`app-tutorial-modal` (all Tier 3), so all six got their restyle pass here, benefiting every future page that reuses them (`config`, `events`, `pinyes`). Real infra bug found and fixed along the way: `apps/dashboard`'s Vitest test target never wired `setupFiles` in `project.json` — `src/test-setup.ts` existed but was silently dead code, so `lib-modal`'s jsdom `showModal()`/`close()` polyfill was never active in any dashboard spec (only `libs/ui`'s own Jest-based tests had it). Fixed by adding the `setupFiles` option; this also fixed two unrelated spec files (`template-list`, `tutorial-modal`) that happened to already exercise dialog code paths without the polyfill ever running. Three modal rewrites onto `lib-modal`: `tutorial-modal` (shared, first real dismissible-tutorial-flow usage — needed a `visible()`-guarded `onModalDismissed()` to avoid double-emitting its own `closed` output when an in-content button's `close()` call cascades into the native dialog's own `close` event), `person-new-modal` and `person-delegate-modal` (both simpler — no owned `visible` state, parent mount/unmount only, so `[open]="true"` fixed). `person-detail`'s delegate-removal confirm dialog converted the same way as communication's delete-confirm: always-rendered `lib-modal` with `[open]="!!signal()"`, not `@if...as`. Real component-API gaps flagged (not forced): `lib-input` has no side-by-side label layout, so `person-detail`'s dense two-column edit form (with a dedicated WI-05 regression test for exactly this responsive behavior) stays raw DaisyUI; `lib-button` has no `.join` group mode, so `pagination`'s page buttons and `person-list`'s Cens/Provisionals toggle stay raw; `lib-badge` isn't a clickable control, so the "Etiquetes" multi-select toggle chips (person-detail) and the selectable user rows (person-delegate-modal) stay raw. `lib-card` has no bordered-underline title style, so the two `person-detail` info cards use it as a bare wrapper with a plain `<h2>` as ordinary content rather than the `title`/`sash` inputs. `lib-badge`'s `color` input (hex → APCA contrast internally) replaced three call sites' manual `getContrastColor()` + `[style.color]` pairing. Two real, not-`lint:tokens`-caught violations fixed by manual review (checklist step 3): `text-blue-500` (raw Tailwind palette, two delegate/manager links) → `link link-primary`; a `'#e5e7eb'` hex fallback for uncolored position chips → `[class.bg-base-300]`. One `lint:tokens`-caught hex literal fixed the same way. Typography: `person-detail`'s own `<h1>` got `font-serif`; `person-list`'s page title already inherited it for free via `app-page-header` (fixed during #4's follow-up). 

**Explicitly not touched:** 4 pre-existing `person-detail.component.spec.ts` failures for an "Enllaça amb usuari existent" (link to existing user account) feature that was never implemented — real, unrelated, pending feature work, not a design-system regression; two of its tests needed a `.closest('.flex-wrap')` fix instead of `.parentElement` (lib-button's `display:contents` host adds a DOM level between a real button and its layout parent) and two dialog-selector tests (`role="alertdialog"` → `dialog[open]`) were fixed as real regressions from the modal rewrite, but the "Enllaça amb usuari existent" failures themselves are untouched, same as every prior page in this rollout. 

**Follow-up, user-flagged, real browser review:** (1) `.btn-ghost:disabled` had no override in `button.component.scss` — only `.btn-ghost.lib-btn-loading:disabled` did — so a plain (non-loading) disabled ghost button fell straight through to DaisyUI's own generic `[disabled]` rule, which grays the *background* solid dark, inconsistent with every role's own disabled treatment (transparent fill, muted text only); fixed with a `.btn-ghost:disabled` rule reusing `--ds-neutral-disabled`. (2) `app-active-filters`'s host was `display:block`, not `display:contents` — when it has no active filters it renders nothing, but the still-present empty block box counted as the first flex item under `person-list`'s `justify-between` row, shoving `app-column-toggle` (the second item) to the far right; fixed by making the host `display:contents` too, so an empty render truly disappears from the flex layout instead of leaving a ghost slot. (3) `lib-badge` gained `clickable`/`selected` inputs (real `<button>` render mode + `clicked` output + ring/opacity toggle-chip styling), closing the gap flagged above for `person-detail`'s "Etiquetes" chips (now `lib-badge clickable`, and its manual `getContrastColor()` call is gone too — `color` handles it). The delegate-modal's selectable user rows were deliberately left raw — richer multi-line list items (name+email+badges), not a simple label chip. (4) `person-detail`'s whole edit-mode form converted to `lib-input`, closing the other flagged gap — deliberately accepting the resulting always-stacked label-above-input layout for now (no side-by-side mode exists) rather than block on it; needed two small `lib-input` extensions along the way: `type="date"` added to `InputType` (birth date, shirt date) and pass-through `min`/`max` inputs (the shoulder-height field's 0–250 range, previously only a native HTML attribute). Both TDD'd in `libs/ui`. 

**Second follow-up round, same review:** found a real Badge bug shipped in the first round — `clickable`'s button branch rendered with *no projected content at all* (confirmed via a repro test before touching anything), because `<ng-content>` had been added to both the `@if`/`@else` branches; Angular resolves each `<ng-content>` selector to one projection point at compile time, not per-branch, so one of the two silently got nothing. Fixed with the same `<ng-template>` + `ngTemplateOutlet` pattern Button/Card already used for this exact reason — now documented as its own gotcha in `DESIGN_SYSTEM.md`'s Component conventions so it isn't rediscovered a third time. Built `lib-form-field` (label/required/hint/error chrome, content-projecting an arbitrary control — not just `lib-input`) after discussing the alternative of a one-off `lib-select` directly: the deciding case was `person-detail`'s "Etiquetes" tag-picker, a fully custom chip-toggle widget no per-control-type component would ever cover. Refactored `lib-input` to compose it (one label implementation, not two to keep in sync), then built `lib-select` on the same foundation — judged worth keeping alongside the wrapper (not merely wrapper + raw `<select>`) since it removes the same recurring boilerplate `lib-input` already removes for text fields (DaisyUI class assembly, `aria-`* wiring, CVA, tap target, error-class toggling). `lib-select`'s CVA `writeValue` needed an `effect()` instead of a template `[value]` binding — the native `<select>`'s own property bindings apply before its projected `<option>`s are inserted, so the browser silently ignores a value with no matching option in the DOM yet. Applied both across `person-detail`'s whole edit form (`Disponibilitat`/`Acollida` → `lib-select`, the "Etiquetes" picker's label → `lib-form-field`) and sized every field `xs` per request, which surfaced that `lib-form-field`'s label text didn't shrink at `xs` either — fixed the same way `lib-input` already had. Simplified both info cards to `lib-card`'s own bare `title` input (plain bold label, no sash) instead of the raw bordered-underline `<h2>`, dropping that visual detail rather than trying to preserve it — the user's own call once told it wasn't achievable without a new card feature. `lib-button-group` (closing the last flagged gap — `pagination`'s/`person-list`'s hand-rolled `.join` segmented controls) was designed but deliberately not implemented: a thin `.join` wrapper plus a new `joinItem`/`active` input pair on `lib-button` itself (same reasoning as `lib-form-field`'s shared `id` — a real class on `lib-button`'s own rendered element, not something the wrapper can inject into an opaque `display:contents` child), mocked up across both themes using the real running colla's own computed token values (published as an artifact, not wired into the app). Also caught and fixed real on-disk corruption in `person-detail.component.html` mid-session (a stray `Informació de la` fragment spliced into an unrelated class attribute, likely a concurrent-edit race) — flagged and fixed rather than silently worked around. 

**Third follow-up round:** added `lib-form-field`/`lib-select` demo sections to `/design-system` (they'd been built but never wired into the live reference page). Converted `person-detail`'s remaining ad hoc labels — the Actiu/Membre/Xicalla toggles and the Observacions tècniques textarea — to `lib-form-field`, closing the last gap between the newly-`xs`-sized edit form and its still-raw-labelled neighbors. Deliberately left the "Compte vinculat"/Responsable/Delegacions block alone after raising the conflict with the user directly: unlike every other field on this form it has no `formControlName` and renders identical content in both view and edit mode, so wrapping it in `lib-form-field` only while editing would mean duplicating all four account-state branches (active/pending/responsable/none) for a purely cosmetic alignment win — not worth it since its current horizontal dt/dd layout already matches every other read-only label's typography. `person-list`'s per-row tag badges (rendered via `app-data-table`'s generic `colorBadges` column type) were never clickable at all — a new feature, not a bug fix in a prior round. Extended the shared `ColumnDef`/`ColumnColorBadge` model (`libs`-adjacent, `apps/dashboard/src/app/shared/models/column-def.model.ts`) with an optional `id` on each badge and an optional `onColorBadgeClick` callback per column; `data-table.component.html` renders a badge as a real `<button>` (with `stopPropagation` so it doesn't also trigger the row's own click-to-navigate) only when both are present, in both the table and card-mode layouts, so `event-participation`'s existing `colorBadges` usage — which doesn't opt in — is untouched. `person-list` wires `onColorBadgeClick` to the same `togglePosition(id)` already used by the Etiquetes filter dropdown, so clicking a person's tag in the table adds (or removes, if already active) that tag from the list's own filter — one shared method, no new state. Deliberately did not reach for `lib-badge` here: its `clickable` mode dims every unselected badge to 40% opacity by default (correct for the Etiquetes toggle-chip picker it was built for), which would make every tag in every row look "deselected" by default in a plain listing table — a categorical mismatch, not a styling tweak.

#### 6. `config`

**Rough size:** hub (`ConfigComponent`, already touched twice this session — low risk) → `users`/`tags`/`seasons` lists (`app-data-table` ×3, `app-column-toggle`, `app-active-filters`) → their 3 form modals → `legal-documents` (ADMIN-only, simplest, ships last)

✅ done — Correction to the row's own premise: only `users` actually uses `app-data-table`/`app-column-toggle`/`app-active-filters` (already Tier 3-restyled during `persons`, reused unchanged here); `tags`/`seasons` turn out to use a raw `<table>`, not `app-data-table` — left as-is (converting them to `app-data-table` is a structural feature change, not a token swap, out of scope here). 

Hub's `border-l-4` cards converted to the `sash="title"` + whole-card `routerLink` pattern already established by the sync/communication hubs; needed 5 new `DOMAIN_ICONS` entries (`USER_COG`, `TAG`, `CALENDAR_RANGE`, `SHIELD_CHECK`, `DESIGN_SYSTEM`). `users`: Cerca → `lib-input`, Rols multiselect → `lib-badge clickable` chips (same toggle-chip pattern as `person-detail`'s Etiquetes, chosen over `lib-select multiple` since 3 fixed roles read better as inline chips than a dropdown), Estat/Accés checkboxes → `lib-form-field` wrapping a plain checkbox (matching `person-list`'s "Sols actius"), both the grant-role and the deactivate/reactivate confirm dialogs → `lib-modal`. `user-form-modal` → `lib-modal`/`lib-input`/`lib-button`; this is the only page in the app that mounts `app-person-search-input`, so it got its first-ever Tier 3 restyle here too: `z-50`→`z-dropdown`, `shadow-lg`→`shadow-overlay`, its result-row badges → `lib-badge`. `tags`/`seasons`: page-header CTA, row actions, and delete-confirm dialogs → `lib-button`/`lib-modal`; `tags`' position-type display badges and `seasons`' "Actual" badge → `lib-badge` (the tags one via `[color]`, replacing a manual `[style.background-color]`/`[style.color]` pairing); their two form modals → `lib-modal`/`lib-input`, and `tag-form-modal`'s preset toggle chips → `lib-badge clickable/selected/[color]`, retiring its manual `getContrastColor()` call the same way `person-detail`'s Etiquetes picker did. `legal-documents` (inline-template, ADMIN-only): its per-type cards → bare `lib-card` wrapper with a plain `<h2>` (same precedent as `person-detail`'s two info cards — no sash, since the header row already carries its own badge cluster), all status/version badges → `lib-badge`, all buttons → `lib-button`, both dialogs (publish-confirm, version-view) → `lib-modal`; the draft textarea and the version-history "Veure historial" toggle stay raw (no `lib-textarea` exists yet; the toggle is a plain text link, not button-shaped). Real bug class caught in 4 places before shipping (same `display:contents`-vs-`space-y-*` issue as `communication`'s and `sync`'s hubs, just recurring in modal `<form>`s this time, not hub grids): `user-form-modal`/`season-form-modal`/`tag-form-modal`'s top-level `<form class="space-y-4">` and `legal-documents`' outer wrapper all had a `lib-input`/`lib-card` as a direct child — margin-based spacing silently collapsed; all four switched to `flex flex-col gap-4`. One flagged, not forced, gap: `lib-input` has no `maxlength` input, so `season-form-modal`'s Nom field dropped the native `maxlength="100"` attribute (the `Validators.maxLength(100)` reactive-form check still enforces it, just without the browser-level stop-typing behavior). Typography: hub and `legal-documents` both hand-roll their own `<h1>`, both given `font-serif`; `users`/`tags`/`seasons` inherit it for free via `app-page-header`, already fixed at its source during the `communication` step. 

Verify: `nx lint`/`build`/`test dashboard` all clean — only the 4 pre-existing `person-detail` "Enllaça amb usuari existent" failures, unrelated to this page, same as every prior rollout step. 

**Real-browser check not done this round**: no ADMIN E2E credentials were available in this session (`apps/dashboard-e2e/src/audit/config-audit.spec.ts` already covers `/config`, `/config/users`, `/config/tags`, `/config/seasons` and just needs `E2E_EMAIL`/`E2E_PASSWORD`); the user explicitly chose to skip it for this round rather than supply credentials or eyeball it manually — a real gap against 7.2's own "real browser check before merge" rule, left open for a follow-up pass.

#### 7. `events` (rehearsals + performances share one feature module)

**Rough size:** list (smallest) → detail → `attendance-list`/`attendance-edit-modal`/`event-form-modal` → `segment-manager` (23 buttons — the single biggest non-`pinyes` file in the app)

✅ done — `segment-manager` was initially deferred (deliberately complex, per user instruction) and completed in a later pass once the rest of the row was done; see its own note below.

Everything else done: `event-list`, `event-detail`, `attendance-list`, `attendance-edit-modal`, `event-form-modal`, plus two components not named in this row's own scope note but genuinely part of the page (`event-participation` — embedded in event-detail's Participació tab, no route of its own; `attendance-confirmation` — independently routed at `/events/:id/confirmation`). `event-detail`'s tab strip was already `lib-tabs` from the earlier cross-cutting Tabs unification (Tier 1 item 7 above) — this pass covers everything else on that page. `event-list`: buttons/inputs/selects/empty-state → `lib-button`/`lib-input`/`lib-select`/`lib-empty-state`, matching `person-list`'s exact filter-bar composition precedent; `space-y-4` → `flex flex-col gap-4`. `event-form-modal`: `lib-modal` fixed-mount (`[open]="true"`, parent `@if`-mounts/unmounts, same pattern as `season-form-modal`); the "fake disabled div" for a preset event type turned out to be unnecessary — the reactive form already calls `form.get('eventType')?.disable()`, so a real `<lib-select formControlName="eventType">` (which implements `setDisabledState`) renders correctly disabled with no special-casing. One real component-API gap: `lib-input`'s `InputType` has no `"time"` — the Hora inici field stays raw. `attendance-edit-modal` (no prior spec file): `lib-modal`, header's position/Xicalla badges → `lib-badge`; the four status-selector buttons stay raw — a real, not-forced conflict, since one segment (PENDENT) uses ghost+`.btn-active`-darken semantics while the other three use fill/outline-color semantics, and `lib-button-group`'s segmented model requires one scheme for the whole group and outright disallows `variant="ghost"` in a `joinItem`. `attendance-list` (the biggest single file): outer `.card` → `lib-card` (bare wrapper, custom header row kept raw since it mixes a title with trailing filter controls — `lib-card`'s `title` input can't express that); retired the page's one live `getContrastColor` call site (3 usages) by switching the position-tag/status/filter-chip badges to `lib-badge`'s `[color]`/`[variant]`; the interactive status-edit badge and the position-filter chip both need `clickable selected` (not just `clickable`) to render solid-filled rather than `lib-badge`'s clickable-default outline-only look, since these represent "the current state," not an unselected option in a toggle set; per-row position tags deliberately stay `clickable`-free plain `[color]` badges wrapped in a raw `<button>` for the click handler, mirroring `person-list`'s established "don't reach for `clickable`'s dim/outline toggle-chip look in a plain listing row" precedent — forcing it here would make every tag in every row read as unselected by default, the same categorical mismatch already flagged there. Real regression caught by the WI-03 tap-target spec: `lib-badge`'s `clickable` mode deliberately never forced a `min-h-6` floor (a documented, discussed trade-off from when Badge was built — "relies on the chips' own gap spacing instead"), so the old `min-h-6`-on-the-status-badge assertion no longer holds; updated the spec to assert the badge renders as a real `<button>` instead of asserting a tap-target class the library never promised here. `event-participation`: same filter-bar/empty-state swap as `event-list`; `text-conflict`/`PILL_CONFLICT`'s `'text-conflict font-bold'` → `text-error` (the `conflict` custom color's phased retirement, called out in Phase 3's Button section — other `conflict` call sites remain, all in the deferred `pinyes` module or the deferred `segment-manager`, so the shared token itself isn't deleted yet); updated the 10 `.class.includes('conflict')` spec assertions to check for `'text-error'` instead. `attendance-confirmation` (kiosk-style touch UI, `/events/:id/confirmation`): initially left the virtual keypad and person-result cards bespoke, then converted both on explicit user follow-up. Keypad keys → `lib-button shape="square"` — `.btn-square`'s fixed 3rem (DaisyUI) fights the keypad's viewport-relative `calc()` sizing, so the wrapping cell overrides it back to fill via `[&_button]:!w-full [&_button]:!h-full` descendant-selector classes (confirmed compiled into output CSS with `!important`); regular keys also get a `bg-base-100`/`text-base-content` override the same way, since `lib-button` has no plain "light surface" variant of its own (backspace keeps a real `variant="error"`, no override needed). Person-result rows → `lib-card`, but **not** its own `clickable` mode: that renders a real `<button>` (inline-block, won't stretch full width like a `<div>` does), so the click/keyboard handling was kept on the wrapping `<div role="button">` instead — the exact pattern `app-data-table`'s own card-mode rows already use, found and matched after the first attempt broke full-width. The "N confirmed" badge's custom `px-3 py-3` sizing still doesn't map to Badge's fixed scale, left raw; the two "Tanca ✕" buttons still have no `lib-button` variant for on-primary-background ghost text, also left raw. Also dropped `font-mono` from the result-card alias: the precedent cited for it (`attendance-edit-modal`'s alias heading) had itself lost `font-mono` earlier in the same rollout when that modal moved to `lib-modal`'s plain-string `title` — the precedent no longer held by the time it was cited. Fixed 3 untokenized `bg-white`/`shadow-sm`/bare-`shadow` instances → `bg-base-100`/`shadow-raised`/`shadow-overlay` (this file had no `@Component.imports` array at all before this pass) and converted the one badge with no custom padding (the Xicalla tag) to `lib-badge`.

**`data-table.component.html` (shared, Tier 3, touched as a side effect of `event-participation`'s user-reported bugs, not its own row):** two real bugs found and fixed, both worth knowing about for any future consumer. (1) The sticky/primary column's `<td>` had no background at all in the actual data row (only the header and skeleton rows did) — scrolling horizontally let other columns' text show through underneath it; first fixed with a `idx % 2`-computed background, which turned out to desync from the real DOM `:nth-child` position whenever a `groupSeparator` row is inserted before an item (exactly the "Events passats" case) — replaced with Tailwind's `group-odd:`/`group-even:` variants on a `.group`-marked `<tr>`, tied to real `:nth-child` CSS so it can never drift (confirmed compiled into output CSS). (2) Added a `getCellTitle()` helper (joins `pills` fragments, since they have no plain `value`) and a `prefix` field on `ColumnDef` (a small coloured glyph before the primary column's text, used by `event-participation`'s conflict marker, previously folded into the plain-text label as an uncoloured glyph) — both driven directly by explicit user feedback on the rendered page, not part of the original migration plan. Primary-column truncation was tried then reverted (user wants the name column full-width); the truncation instead applies to the *other* columns via `max-w-[130px]` + a `title` tooltip.

**`app-stat-card` (shared, Tier 3, touched as a side effect too):** converted to genuinely wrap `<lib-card>` internally rather than DaisyUI's raw `.stat` classes, per explicit user ask — `icon` input changed from a string name to `LucideIconData` to match `lib-card`'s own convention, rippling to its one consumer (`event-detail`'s three summary tiles).

`segment-manager` (the deferred piece, done in its own later pass — 23 buttons, the single biggest non-`pinyes` file in the app, sitting at the `events`/`pinyes` domain boundary: an `events`-feature component that imports one `pinyes`-feature component, `FigurePickerModalComponent`, left untouched as its own separate migration unit): converted the buttons/badges/modals that gained *unambiguous* accessible-name coverage from a real component `@Input` (`ariaLabel` on `lib-button`/`lib-badge`, which correctly reaches the real rendered element regardless of the `display:contents` host) and kept everything else raw where a genuine, verified library gap existed — a stricter application of "flag, don't force" than most pages needed, because this component's spec is unusually DOM-selector-heavy (dozens of exact `[aria-label="..."]` strings, a `[data-instance-id]` + `.click()` test, a `select[aria-label]` query, a compound `.btn-ghost.btn-xs[title*="..."]` selector). Kept raw, each for a real, verified reason: both CDK drag handles (`cdkDragHandle` needs a real hittable box — `display:contents` has none, so pointer events could never reach it); the collapse toggle and visibility toggle (need `aria-expanded`/`aria-pressed`, and `lib-button` has no input for either — an attribute bound directly on the tag lands on its boxless host, not the real button, so it wouldn't reliably reach assistive tech even though it'd still satisfy a `querySelector`); the segment-rename and instance-name trigger buttons (plain-text-styled by design, and the instance one carries `data-instance-id` that a spec `.click()`s directly — same host-vs-real-element gap, and calling `.click()` on `<lib-button>`'s own host wouldn't fire its `(clicked)` output, since that's wired to the *inner* button, not the host); the figure-mode `<select>` (`lib-select` has no `ariaLabel` input at all, and the spec queries `select[aria-label]` on the real element); the segment-rename `<input>` (`lib-input` has no `ariaLabel` input either, and its `label` input would render a *visible* label, breaking the compact inline row). Converted cleanly: the header toolbar buttons, the Pinyes/Troncs view toggle (a genuine `lib-button-group` `joinItem`/`active` fit, matching `person-list`'s Cens/Provisionals precedent), the rename save/cancel buttons, add-figure/assign/project/delete-segment/copy-instance/delete-instance buttons (colour nuances DaisyUI classes previously baked in via `text-error`/`text-secondary`/opacity — `lib-button` has no per-instance color override, so these are reproduced with a wrapping `<span>` carrying the color/opacity class, which cascades or paints through the `display:contents` host correctly since `color`/`opacity` aren't box-dependent the way a background or margin is), the people/conflict pills, the "Neta" badge, and two of the three inline modals. The order-number circle and the copy-modal's mirror of it were *tried* as `lib-badge` then reverted: `lib-badge`'s `ariaLabel` input is only wired in `clickable` mode (a real, previously-undocumented gap found here) — on a static badge it silently drops the accessible name, and its literal `.badge` class also collided with the "Neta" badge in a `querySelector('.badge')` first-match test, breaking two tests at once; reverted to the original raw numbered-circle `<span>`, which was never really a "badge" semantically anyway. Two of the three inline confirmation modals (remove-instance, mode-change) are real `role="alertdialog"` dialogs with `aria-describedby` — a second real, newly-found `lib-modal` gap (it only wires `aria-labelledby` to its own generated title, nothing else) — kept as raw `<dialog>` shells with their *inner* buttons still converted to `lib-button`; the third (copy-to-segment, no alertdialog/describedby needs) converted cleanly to `lib-modal`. `shadow-sm` on the outer wrapper retired for free by switching it to a bare `lib-card`. Verify: `nx build`/`lint`/`test dashboard` clean for every file this pass touched — the only lint errors in the whole app are 4 pre-existing ones in `data-table.component.html` from an unrelated `onCellClick` feature that landed in the same commit via a concurrent session, not this pass; the only test failures are the same 4 baseline `person-detail` ones plus 2 pre-existing, already-committed `event-participation` navigation failures (a `tab` query-param feature, also unrelated to this pass) — `segment-manager`'s own spec: 0 failures.

**Follow-up, user-requested — closed 3 of the flagged gaps at the library level instead of leaving them raw forever.** Added `ariaExpanded`/`ariaPressed` inputs to `lib-button` (bound on the real rendered `<button>`/`<a>`, absent — not `"null"` as a string — when unset) and an `ariaLabel` input to `lib-select`/`lib-input` (bound on the real native `<select>`/`<input>`, for compact label-less usage where `label`'s visible-text rendering via `lib-form-field` would break an inline row) — all three documented in `docs/DESIGN_SYSTEM.md`'s own component tables and covered by new `libs/ui` spec tests (`ariaExpanded`/`ariaPressed` absent-by-default + set cases for Button, `ariaLabel` absent-by-default + set cases for Select and Input). Converted the collapse toggle, the visibility toggle, the figure-mode `<select>`, and the segment-rename `<input>` to use them. The visibility toggle also got a real `ariaLabel` in the same text as its old `title`-only tooltip (title-only tooltips aren't reliably read by screen readers/touch) — the compound `.btn-ghost.btn-xs[title*="..."]` spec selector that depended on the tooltip was updated to match on `[aria-label*="..."]` instead, a stricter selector than before, not a workaround. The drag handles, the two plain-text-styled trigger buttons (segment rename, instance name — `data-instance-id` + a spec `.click()` target), and the two `alertdialog`/`aria-describedby` confirmation modals remain raw, unaffected by this round — those gaps are structural (a real hittable box for CDK, a host-vs-real-element split for a `.click()` target, dialog semantics `lib-modal` doesn't model), not just missing an `aria-*` input.

**Follow-up, user-requested — figure-instance renaming, a genuinely new feature, not a token/markup swap.** `InstanceDetail.label` (nullable, user-settable) already existed on the model and `FigureInstanceService.update()` already `PUT`s it via `UpdateInstancePayload.label` — this wired up a UI for a real, already-supported backend capability, not a speculative one. Added a small pencil `lib-button` next to each figure instance's name (hidden when locked, matching every other per-instance mutating control); clicking it swaps the name+pinya-count trigger button for an inline `lib-input` + save/cancel `lib-button`s, mirroring the existing segment-rename flow's `editingSegmentId`/`editingName` signal pair exactly (`editingInstanceId`/`editingInstanceName`, `startEditInstance`/`cancelEditInstance`/`saveInstanceName`) — same optimistic-update-then-reconcile pattern already used for `updateFigureMode`/`saveSegmentName`, not a new one invented for this. 

Verify: `nx lint`/`build`/`test dashboard` all clean across every file in this pass — only the same 4 pre-existing unrelated `person-detail` failures throughout. 

**Real-browser check not done this round** — same gap as `config`'s row, not re-raised with the user this time; left open.

#### 8. `pinyes`

**Rough size:** 112 raw buttons, 36 inputs, ~11 modal files across 21 template files — by a wide margin the biggest module

Last, as already agreed. Order within it: `template-list` ✅ done → `template-editor` ✅ done → `composition-editor` ✅ done → `lib-checkbox`/`lib-textarea` built and every existing raw call site across both apps migrated ✅ done (Tier 1 #8/#9 above) → `segment-workspace` (5 tabs, list below) ✅ done → `projection-view` (not yet started).

**Canvas (Tier 5) rescheduled, superseding the original plan.** This row's Konva canvas restyle (`libs/pinyes-render`'s `app-figure-canvas`/`app-tronc-view`) was originally meant to land inline with `segment-workspace`, on the reasoning that it's shared with the PWA's projection view so doing it here would cover most of 7.3's segment-projection screen for free. Superseded by explicit user call: the canvas now becomes its own phase *after* the full PWA rollout (7.3), not before it — see the new 7.3.5 entry below. `segment-workspace`'s five tabs were completed with the canvas itself (`app-figure-canvas`, `app-tronc-view`) and every Tier 5 in-canvas element explicitly excluded, per each row's own note below.

`template-list` ✅ done — covers both its own tab (`template-list.component`) and the `composition-grid-tab` sub-view (the old `figure-list-tab` was confirmed dead code and deleted). Tier 1/2 swap: `lib-button`/`lib-badge`/`lib-card` (`sash="title"`)/`lib-input`/`lib-modal`/`lib-tabs`/`lib-empty-state`, plus `app-page-header`. New net-new piece, not a primitive swap: `TemplatePreviewDrawingComponent` — a shared roughjs "sketch" drawing of a figure's (or composition's) tronc silhouette, replacing the old decorative gradient panel; draws bracing pinya people (secondary color) and a ground line when the figure `hasPinya`, purely additive personality work in the spirit of the paper/sketch identity principle, not itself a design-system primitive. Component-API gap flagged (checklist step 5): `lib-button` has no compact "icon-only on narrow, label on wide" responsive mode, so the card action row (Duplica/Elimina/Edita) hand-picks `shape="square"` for the two lower-priority actions instead.

`template-editor` ✅ done — one component at a time, per the plan below; the Konva canvas itself (`app-figure-canvas`/`app-tronc-view`, Tier 5) stayed explicitly out of scope for this pass. **1. name-prompt modal** (`showNamePrompt`) → `lib-modal` + `lib-input` + `lib-button`, same pattern as `template-list`. **2. shortcuts modal** → `lib-modal` (`size="lg"`), the five `<section>`/`<kbd>` shortcut blocks kept as projected body content unchanged. **3. top-bar buttons** (undo/redo, preview toggle, shortcuts/help triggers) → `lib-button`. **4. properties panel form controls** → `lib-input` for label/indicator (`maxLength` capped at 6)/width/height/x/y/z; the shape field → `lib-select`. Rotation stayed a raw `<input type="range">` (flagged: `lib-input`/`lib-select` have no range mode, deferred per `docs/DESIGN_SYSTEM.md`). The PINYA position-type dropdown (per-option color-swatch + label) turned out not to need a bespoke listbox after all: `lib-select` gained a `swatchColor`/`swatchShape` input pair (a decorative dot rendered in the closed control, independent of browser support for rich `<option>` content/`appearance: base-select`) — a real, reusable primitive addition rather than an escape hatch. **5. left toolbar** (Base/Pinya node-add tiles, snap-to-grid toggle) — converted after a second pass, not left raw as first planned: the tiles → `lib-button` with a shared `.toolbar-btn-content` icon+label wrapper; only the snap-to-grid tile stayed raw, and only because its 3-word Catalan label ("Encaixa a la quadrícula") can't fit `lib-button`'s fixed-height box in a ~48px column without either clipping or truncating the text — both rejected by the user — so it kept the DaisyUI `.btn` markup but gained the shared `.ds-lift`/`libDsLift` hover-lift motion directly, for full visual/animation parity with a real `lib-button` (flagged as a genuine "fixed-height button can't fit a long wrapped label in a narrow column" API gap). `node-dpad`/`node-actions` (shared controls, `shared/components/controls/`) converted alongside: D-pad mode/step toggles → `lib-button-group`/`lib-button`, arrow/rotate buttons → `lib-button` wrapped in a plain pointer-event `<div>` (component-API gap: `lib-button` exposes only `(clicked)`, no raw pointer events, needed for the press-and-hold repeat gesture); Accions (Duplica/Estén rengla/Elimina) → `lib-button` with an icon-over-label stacked layout to avoid the same fixed-height clipping. Two bonus areas surfaced by the user mid-pass and folded in: **rengla-overlay**'s floating action bar (Rengla nova/Finalitza/Cancel·la/Suprimeix/close-X → `lib-button`, "Primer cordó" number field → `lib-input`) and **tronc-view**'s (`libs/pinyes-render`) selected-node panel (Etiqueta/Indicador → `lib-input`, delete → `lib-button`, Tipus preset-tags → `lib-badge` with `[color]`, Pos./Amp. stepper +/− → `lib-button`) — the latter needed a `pinyes-render` Jest config fix (`transformIgnorePatterns` didn't reach `@muixer/ui`'s ESM-only `culori`/`apca-w3`/`colorparsley` deps through pnpm's nested `node_modules/.pnpm/` layout) that unblocks any future `@muixer/ui` usage in that lib, not just this one. Shared-component additions landed along the way, each a genuine reusable gap rather than a one-off: `lib-input.maxLength`, `lib-select.swatchColor`/`swatchShape`. Both modals converted the same old CSS-only `.modal-open` convention already flagged module-wide (§7.2 "the modal rewrite") onto `lib-modal`. Verify: `nx lint`/`build`/`test dashboard`+`ui`+`pinyes-render` all clean — only the pre-existing unrelated `event-list` "pre-selects the active season" date-flake failing throughout.

`composition-editor` ✅ done. **Top bar** rebuilt to match the template editor's own (`.editor-topbar` layout, `lib-button` for back/snap-toggle) minus the mode tabs it doesn't have, plus a new inline-rename affordance (pencil `lib-button` swaps the static title for `lib-input` + accept/cancel `lib-button`s, Enter/Escape wired). **Left bar** left structurally as-is per explicit user call — only its search field → `lib-input` (leading icon via its `icon` input). **Right properties panel** (`figure-properties-panel` — shared with `segment-workspace`'s Distribució tab, so this conversion covers that consumer too): Nom → `lib-input`, Mode switcher → `lib-button-group`/`lib-button[joinItem][active]`, Cordons stepper +/− → `lib-button`, X/Y → `lib-input[type=number]`, Angle → a raw range slider like the template editor's rotation control (same documented gap), ranged −180..180 rather than 0..360 since this angle comes from a free rotate-drag on the canvas and can go negative. The cordons-oberts checkbox stayed raw for now (no `lib-checkbox` in the library yet — to be migrated once it's built, see Tier 1 #8 below). Shared-component addition: `lib-button-group.fullWidth` — DaisyUI's `.join` is `display: inline-flex` (sized to content, not its container) so a segmented control meant to fill a narrow sidebar had nothing to stretch against; paired with `fullWidth` on each `joinItem` child (each wrapped in its own `flex-1 min-w-0` element, since `lib-button`'s `display: contents` host can't itself be a flex item) it now splits the row evenly — documented in `docs/DESIGN_SYSTEM.md` with the full pattern. Verify: `nx lint`/`build`/`test dashboard`+`ui` all clean — same pre-existing `event-list` flake only.

**`segment-workspace` (5 tabs) ✅ done, canvas excluded.** Real `grep`/read pass over `segment-workspace/` + the sub-components its tabs compose (2026-09-01) — Konva itself (`app-figure-canvas`, `app-tronc-view`, Tier 5) and `previsualitza-tab` (a bare `<app-projection-view [embedded]="true">`, entirely that later plan row's concern) stay out of scope, per the same rule as `template-editor`'s canvas exclusion — and, per the rescheduling above, Tier 5 stays out of scope for the *whole* `pinyes` sub-phase now, not just this row:

- **`segment-workspace.component`** ✅ done (shell: header + tab strip + prev/next nav). 4 raw buttons (back, prev/next segment, help) → `lib-button`; the "Bloquejat" lock indicator → `lib-badge`. Tab strip is already `lib-tabs` (done ahead of schedule, see the Tabs entry above). Two user-driven layout changes landed in the same pass, ahead of `pinyes-tab`'s own turn below: the `<h1>` segment title is now a fixed `w-48 shrink-0 truncate` (so the prev/next arrows sit at a stable position regardless of title length), and `pinyes-tab`'s old footer (Reinicialitza/Importa pinya) was relocated into this shell's header, pushed to the far right with `ml-auto`, gated to `activeTab() === 'pinyes'`. Since those two actions now live in this file, their full checklist pass (not just the relocation) happened here rather than waiting for `pinyes-tab`'s row: both raw `.dropdown` figure-picker menus and the raw reset-confirmation `<dialog>` converted to `lib-modal` + `lib-button`, per the resolved dropdown/menu discussion below — `app-import-pinya-modal` itself (the raw `<dialog>` opened once a target figure is chosen) stays untouched, still `pinyes-tab`'s row's concern. Typography: `<h1>` got `font-serif`, consistent with the established precedent. Component-API gaps flagged, not forced: `lib-badge` has no `role`/live-region passthrough, so the "Bloquejat" badge lost its `role="status"` announcement; `lib-modal` has no `alertdialog` role override, so the reset-confirmation dialog lost `role="alertdialog"`/`aria-describedby` (its close-on-backdrop-while-resetting behavior incidentally got *stricter* — `dismissible` now ties to `!resetting()` — not a regression). Verify: `nx lint`/`build`/`test dashboard`+`ui` all clean, same pre-existing `event-list` flake only.
- **`segment-conflict-panel`** ✅ done (cross-tab conflict banner) — as straightforward as expected. Mostra/Amaga toggle → `lib-button variant="error" outline size="xs"`; "Marca com revisat" → `lib-button variant="ghost" size="xs"`; prev/next scroll arrows → `lib-button variant="ghost" size="sm" shape="circle"`; per-placement view icon → `lib-button variant="ghost" size="xs" shape="square"`, remove icon → `lib-button variant="error" ghost size="xs" shape="square"` (the exact "destructive icon button in a dense card row" case `ghost`'s own doc comment names); `TRONC`/non-`TRONC` area badges → `lib-badge [variant]="area === 'TRONC' ? 'error' : 'ghost'" size="sm"`. Every `lib-button`/`lib-badge` that needed a margin/shrink utility (`ml-auto`, `shrink-0`, the mobile-hide `hidden lg:block` on the scroll arrows) got a thin wrapping `<span>` instead of putting the class on the `<lib-*>` tag itself — inert there, `display: contents` host, see Component conventions. **New Tier 1 addition, this pass:** `lib-button` gained an `ariaControls` input (same `input<string>()` shape as `ariaLabel`, forwarded via `[attr.aria-controls]`) — the Mostra/Amaga toggle is a real disclosure control pointing at `#segment-conflict-panel-body`, and no existing input covered it (`ariaExpanded`/`ariaPressed` are booleans, this needed an id reference); documented in `docs/DESIGN_SYSTEM.md`'s `lib-button` entry. Verify: `nx lint`/`build`/`test dashboard`+`ui` all clean, same pre-existing `event-list` flake only.
- **`pinyes-tab`** ✅ done. Undo/redo icon buttons → `lib-button` (their floating toolbar's own untokenized `shadow` → `shadow-raised` while touching that markup, checklist step 3). The two footer actions (Reinicialitza, Importa pinya) were already relocated into `segment-workspace.component`'s header ahead of this row's own turn (see that entry above) — their raw `.dropdown` figure-picker menus and the raw reset-confirmation `<dialog>` were converted to `lib-modal` + `lib-button` there, per the resolved dropdown/menu discussion below, so nothing remained here to redo. `app-import-pinya-modal` (was a fully raw `<dialog>` + a `role="listbox"` history list) → `lib-modal size="lg"` + `lib-button` for Tancar/Importa, `lib-badge variant="ghost" size="xs"` for the per-entry assignment-count chip; its own manual close-X/backdrop-click handling (`onBackdropClick`) is now `lib-modal`'s job, deleted. `app-already-assigned-dialog` (was raw, inline template in the `.ts`) → `lib-modal` + `lib-button`; its old effect()+viewChild autofocus hack (needed only because the previous dialog was CSS-only, not a real `showModal()`) is gone too — replaced by a new `lib-button` `autofocus` input (see Tier 1 `lib-button` entry, added this same pass) on "Moure ací". First pass mistakenly flagged `already-assigned-dialog`'s "Assignar igualment"/TRONC badge as a genuine `lib-*` API gap (a "custom `conflict` domain color no variant covers") — user-caught: the plan had already decided (Tier 1 Button section above) to retire `conflict` in favor of `error` project-wide, not preserve it as a permanent one-off color. Fixed in the same pass as the full `conflict`→`error` retirement (see that entry above): "Assignar igualment" → `lib-button variant="error" outline`, the TRONC placement badge → `lib-badge variant="error"` (wrapped in a plain `<span class="shrink-0">`, since a layout class on `<lib-badge>` itself is inert — `display: contents` host), the tronc-warning line → `text-error`. Two real gaps remain, unrelated to color: `import-pinya-modal`'s result banner (`alert`/`alert-error`/`alert-success`) stays raw — no `lib-alert` primitive exists yet; its selectable history-list rows (border/bg selected-state language) stay raw too, same judgment call as `person-panel`'s per-person rows (a domain-specific selectable-row pattern `lib-button` doesn't model, not a gap). Verify: `nx lint`/`build`/`test dashboard`+`ui` all clean, same pre-existing `event-list` flake only.

**New Tier 1 addition, this same pass:** `lib-button` gained an `autofocus` input (imperative `effect()`+`viewChild`, same pattern as `lib-input`/`lib-textarea`'s own) — a plain native `autofocus` attribute on `<lib-button autofocus>` is inert (the `display: contents` host can't forward it down to the real inner `<button>`/`<a>`), and `already-assigned-dialog`'s "Moure ací" is a real, immediate need for it (the habitual default action of a `lib-modal` that should already have focus the instant it opens). Documented in `docs/DESIGN_SYSTEM.md`'s `lib-button` entry.
- **`troncs-tab`** ✅ done. Undo/redo icon buttons → `lib-button variant="ghost" size="xs" shape="square"` (their floating toolbar's untokenized `shadow` → `shadow-raised` while touching that markup). Minimap-toggle circle button → `lib-button variant="neutral" size="sm" shape="circle"`, wrapped in a plain `<span>` for its `absolute bottom-3 right-3` positioning — inert directly on `<lib-button>`, `display: contents` host. The minimap itself is pure SVG (figure boxes/labels) — no primitive applies, left untouched, but its color scheme was brought in line with the tronc panels it mirrors: box `fill`/`stroke` now use the same alpha levels as a tronc panel's own `color+'0d'` background / `color+'55'` border (was a flat `color+'33'` fill and full-opacity stroke — visibly brighter than the panels for the same figure). Reuses `app-already-assigned-dialog` (already covered above). Verify: `nx lint`/`build`/`test dashboard`+`ui` all clean, same pre-existing `event-list` flake only.
- **`nodes-tab`** ✅ done. Figure-selector rail (was a vertical list of `btn-block` buttons) → `lib-button-group[vertical][fullWidth]` + `lib-button[joinItem][active][fullWidth]` (same pattern this pass's `fullWidth` addition was built for), each `joinItem` wrapped in its own `w-full` `<div>` — `lib-button`'s `display: contents` host has nothing of its own to stretch, same reasoning as `composition-editor`'s mode switcher. Ad-hoc/decoration preset grids → `lib-button variant="ghost" size="xs"` (each keeps its own color-swatch content projected inline, no gap — unlike the template editor's left-toolbar tiles, these are plain-label buttons with a small leading dot, not a dense icon+label rail); the placement-mode banner's inline "Cancel·la" → `lib-button` too. Comodí label prompt and delete-confirmation dialogs → `lib-modal` + `lib-button`; the comodí dialog's `<textarea>` was already `lib-textarea` (built ahead of this page, Tier 1 #9). `app-ad-hoc-node-properties` (the properties panel for a selected manual node) is structurally a near-duplicate of the template editor's own node properties panel and converted the same way: `lib-input` for Etiqueta (non-decoration nodes)/Amplada/Alçada/X/Y, `lib-textarea` for Etiqueta on decoration nodes, `lib-select` for Forma, raw range slider for Rotació (existing documented gap), header's Duplica/Elimina/Tancar + the assignment strip's Desassigna + "Treure fons" → `lib-button` (Elimina/Desassigna use `variant="error" ghost`, the exact "destructive icon button in a dense row" case `ghost` was built for), attendance chip → `lib-badge` (its `attendanceBadgeClass` computed, returning raw `badge-*` strings, was renamed `attendanceBadgeVariant` and now returns a bare `BadgeVariant`). `app-color-picker` stayed out of scope (its own already-shared component, not part of this pass). **New Tier 1 addition, this pass:** `lib-input` gained a `blurred` output, identical in shape and rationale to `lib-textarea`'s own (added specifically anticipating this — see that entry's doc comment) — the ad-hoc node label runs the same live-preview-then-commit-on-blur flow the decoration-label textarea already used, and `(blur)` on `<lib-input>` itself doesn't fire (native `blur` doesn't bubble through a `display: contents` host). Documented in `docs/DESIGN_SYSTEM.md`'s `lib-input` entry. Flagged, not forced: the figure-selector rail's labels are center-aligned now (DaisyUI's own `.btn` default; `lib-button` has no `justify`/content-alignment input), where the original raw markup left-aligned them with `justify-start` — a minor visual difference for a narrow single-line list, not worth a new input for a one-off. Verify: `nx lint`/`build`/`test dashboard`+`ui` all clean, same pre-existing `event-list` flake only.
- **`distribucio-tab`** ✅ done — as thin as expected. Reuses `figure-properties-panel` (done, see `composition-editor` above) and `app-figure-canvas` (Tier 5, excluded), so only its own markup needed touching: the floating "Optimitza distribució" reset button → `lib-button variant="error" outline size="sm"`, wrapped in a plain `<span>` for its `absolute bottom-3 right-3` positioning (inert directly on `<lib-button>`, `display: contents` host); its two confirmation dialogs (cordons reduction, cordons oberts disable) → `lib-modal` + `lib-button`, same `role="alertdialog"`/`aria-describedby` gap already documented at `segment-workspace.component`'s own reset-confirmation dialog (`lib-modal` has no alertdialog role override). Verify: `nx lint`/`build`/`test dashboard`+`ui` all clean, same pre-existing `event-list` flake only.
- **`person-panel`** ✅ done (search + filter + person-list side panel, shared by `pinyes-tab`/`troncs-tab`/`nodes-tab`'s node-assignment flow) — the biggest piece. Refresh icon button, Desassigna, tag-filter clear (✕), and the three disclosure toggles ("A la pinya"/"Al tronc"/"Altres") → `lib-button`; search input, height-filter number input, and the tag-filter's inline search input → `lib-input`; Xicalla filter → `lib-checkbox` (built ahead of this page, see Tier 1 #8 above); Max/Min height-sort toggle → `lib-button-group`/`lib-button[joinItem][outlineMode][active]`; assignment badges (`assignedBadgeLabel`) → `lib-badge`. The height-filter `lib-input` needed a small workaround: the pre-existing "don't steal focus from the height input while typing" check used a `ViewChild`+`nativeElement` reference, which a `display: contents` host doesn't expose — replaced with a `heightFocused` signal driven by `(focusin)`/`(focusout)` on a wrapping `<span>` (those bubble past `display: contents`, unlike `focus`/`blur`). The tag filter's own trigger button → `lib-button`; its dropdown (search box + scrollable tag list) is the same "modal with a list" shape as `pinyes-tab`'s two menus above, but stayed a raw `.dropdown`/`.menu` (not `lib-modal`) on reflection — its `z-40`/`shadow-lg` were tokenized to `z-dropdown`/`shadow-overlay` instead. The dense per-person list rows (color-dot + alias + notes icon/emoji + height, selected/hover/opacity states) are a **judgment call to leave raw**, not a gap: they're a domain-specific selectable-list-row pattern `lib-button` doesn't model (same reasoning as the template editor's left-toolbar tiles) — force-fitting `lib-button` here would mean re-deriving its selection styling from scratch inside `<ng-content>`, for a component that would end up used nowhere else. Same judgment call applied to `PersonHoverCardComponent` (`app-person-hover-card`, `libs/pinyes-render`) in a follow-up pass — the single shared hover-tooltip component used by canvas node-hover, `tronc-view`, and this page's own person list: its status chip and per-position tags were hand-rolled `badge badge-xs` spans with a manual `getContrastColor()` call, converted to `lib-badge` (`[variant]` for the status chip via a new `statusBadgeVariants: Record<AttendanceStatus, BadgeVariant>` map, `[color]` for the position tags — `lib-badge`'s own `color` input already computes contrast text, so the manual call was deleted); its floating-tooltip shadow also moved `shadow-lg` → `shadow-overlay`. Verify: `nx lint`/`build`/`test dashboard`+`ui`+`pinyes-render`+`pwa` all clean, same pre-existing `event-list` flake only.
- **Dropdown/menu — resolved, no new primitive.** Every raw `.dropdown`/`.menu` instance found across both apps (`pinyes-tab` ×2, `person-panel`'s tag filter, plus two outside this page's scope — dashboard's `user-chip` account menu, PWA's `person-switcher`) is either a one-shot action list or a short searchable pick-list — both are just `lib-modal` sized down (`xs`/`sm`) with buttons or a search box + list as body content, not a distinct value-holding control the way `lib-select` is. No `lib-dropdown` gets built. `pinyes-tab`'s two menus did convert to `lib-modal` on their own turn (see that row above). **`person-panel`'s tag filter deviated from this on reflection** — its anchored-to-trigger positioning (not a centered overlay) reads more like a real popover than a modal dialog, so it stayed a raw `.dropdown`/`.menu`, just with its `z-40`/`shadow-lg` tokenized to `z-dropdown`/`shadow-overlay`; `user-chip`/`person-switcher` are still open, to be judged the same way (raw-but-tokenized vs. `lib-modal`) whenever their Tier 3 pages get touched.





Each page's pass covers swapping in Tier 1/2 primitives *and* restyling any Tier 3 single-app component that page is the *first* to touch in this sequence — `app-data-table`, `app-filter-bar`, `app-active-filters`, `app-column-toggle`, `app-pagination`, `app-stat-card` (`shared/components/data/`), `app-header`/`app-tab-nav`/`app-user-chip` (`layout/`), `app-color-picker`/`app-emoji-picker`/`app-person-search-input` (`forms/`), `app-node-actions`/`app-node-dpad` (`controls/`, `pinyes`-only), and the two standalone `privacy-consent-modal`/`tutorial-modal` — one visit, not two. Each increment gets a real browser check before merge.

**Per-page checklist.** "Migrate this page" means more than swapping component tags — established while working through `/home`, where the initial pass swapped components cleanly but missed a stale `shadow-sm` and never considered the serif heading question until asked directly. Every page in 7.2/7.3 goes through all of these, not just the first:

1. **Swap Tier 1/2 primitives** where they genuinely fit (Button/Badge/Card/Input/Modal/Toast/EmptyState). Watch for the `display: contents` gotcha every `lib-`* host has (see `docs/DESIGN_SYSTEM.md`'s Component conventions) — a parent's `space-y-*` silently stops spacing these components apart, and sizing classes on the `<lib-*>` tag itself do nothing; both bit `/home`.
2. **Restyle Tier 3 components** this page is the first to touch (list above).
3. **Hunt hardcoded values the swap alone won't catch** — run `pnpm run lint:tokens` scoped to the page's files (it's warn-only in CI, but nothing stops running it locally per-page), then manually check for the kind of thing it *won't* flag: DaisyUI's own untouched defaults masquerading as fine because they're not raw hex — `shadow-sm`/`shadow-md` instead of `shadow-raised`/`shadow-overlay`/`shadow-modal`, an un-tokenized transition duration, spacing that should reference `RADIUS`/`SHADOW` rather than a DaisyUI default that happens to look similar.
4. **Typography review** — does this page have a real display heading (`<h1>`, a section title) that's a candidate for `font-serif`? Once precedent is set (as of `/home`'s greeting), apply it *consistently* to equivalent headings on later pages rather than re-litigating per page — but still worth a deliberate glance, not a silent skip. `font-legible` is Tier 5/canvas-only, not a dashboard-page concern.
5. **Flag genuine component-API gaps instead of forcing a fit.** `/home` hit two: `lib-button` has no `routerLink`/`href` mode (so link-styled CTAs stay raw anchors until that's designed), `lib-card` has no background-tint override (so utility/alert-style banners stay raw `.card` divs). Don't silently hand-roll around a gap *or* silently force content into a component shape it doesn't fit — name the gap, defer or raise it explicitly (as its own decision, not bundled into an unrelated page's review).
6. **Verify**: `nx lint`, `nx build`, and `nx test` for every affected project, then a real browser check (already required — see 7.1) before merge.

**The modal rewrite is the single most disruptive Tier 1 swap, and it's spread across nearly every page above.** ~20 `*-modal.component.ts` files across the app today use either the old CSS-only `.modal-open` convention or a raw `<dialog>` with no real focus trap — none of them have an API `lib-modal` can drop-in replace by class-name find/replace; each needs its open/close logic rewritten onto `lib-modal`'s `open` input + `closed` output convention (see `docs/DESIGN_SYSTEM.md`'s `lib-modal` section). Budget real time for this per page, especially `pinyes` (11 files) and `events`/`config`/`persons` (2–3 each).

**7.3 — Sub-phase C: PWA rollout.** Same pattern; smaller surface. Real feature list per the current app (not just what [CLAUDE.md](../../../CLAUDE.md) documents — `dependents` and `news` exist but aren't in its PWA section yet, a pre-existing doc gap worth fixing in a future pass, not blocking this one): `auth` (3 files) → `home` (1 file, smallest) → `events` (5 files, includes segment projection — its Konva canvas stays raw here too, per the 7.3.5 rescheduling below; only its surrounding chrome is this row's job) → `profile` (3 files — no longer the placeholder CLAUDE.md describes) → `dependents`/`news` (undocumented, audit their real size before sequencing — not yet measured here). Toast is already globally swapped in 7.1, so no PWA page needs to touch it.

**7.3.5 — Sub-phase D: Konva canvas (Tier 5), after the PWA rollout.** Added by explicit user call once `segment-workspace` wrapped, superseding 7.2's original plan of folding the canvas restyle into that row. Runs after 7.3, not before it — both apps' surrounding chrome (dashboard's `pinyes` pages, the PWA's segment-projection screen) will already be fully on tokens by then, so this phase is purely `libs/pinyes-render`'s Konva draw calls: `app-figure-canvas`'s 64 hardcoded hex literals (zone colors, selection/conflict strokes, attendance-status colors reinventing `success`/`warning`/`error`) and `app-tronc-view`'s own, plus the qualitative figure-distinguishing palette (`figure-palette.util.ts`) and panel background/shadow — the full non-DOM color inventory from §1.4/§2.3. CSS custom properties don't reach Konva draw calls, so this can't ride along with a CSS token pass; it needs its own dedicated pass reading the same token values in TS. `CONFLICT_STROKE` in `figure-canvas.component.ts` is this phase's job too (flagged, not touched, when `conflict` was retired to `error` during the `pinyes-tab` pass — see that entry above).

**7.4 — Ratchet** `lint:tokens` **to hard-fail.** Once 7.2/7.3's Tier 3 restyling and 7.3.5's canvas migration have cleared the drift `pnpm run lint:tokens` (§6.1) currently only warns about, flip that CI step from warn-only to a real failing check — closes the loop opened in §6.1 (warn-only was explicitly temporary, pending this) and the Open Questions §3 resolution. Re-run it first to confirm the finding count is actually zero (or down to only justified, commented exceptions) before flipping.

**7.5 — Close-out per sub-phase.** Update `docs/MAP.md`/`docs/DATA_MODEL.md` if entities touched (unlikely here), update `docs/DEBT.md` for anything intentionally deferred (e.g. "PWA UI doc not yet written"), remove any now-obsolete rows.

---



## Open questions (need the user's input, not assumed)

1. **DaisyUI's role** — stays as the component foundation (tokens layer on top), or does the visual identity need custom components DaisyUI can't express, requiring more components built from scratch on plain Tailwind? *(Phase 3 concern — not needed to implement Phase 2.)*
2. **PWA UI doc (4.3)** — worth creating once PWA has enough shared-library structure, or is `DESIGN_SYSTEM.md` + `DASHBOARD_UI.md`'s trimmed-down layout content enough? *(Phase 4 concern — not needed to implement Phase 2.)*

Resolved: **library name** is `libs/ui` (§3.1); **Storybook** is not part of this rollout (§5.3); **CI enforcement strictness (6.1)** is warn-only for now — ratchet to hard-fail once Tier 3/5 drift clears (§6.1).

## Success criteria

- Every color/type/spacing/radius/shadow/motion value in both apps traces to a named token — zero raw hex/arbitrary values outside the token definition file (Phase 1's baseline count reaches zero).
- `libs/ui` is the single implementation of shared primitives; PWA and dashboard visibly consume the same components.
- Light and dark are both fully implemented, token-driven, and switchable at runtime with no missing/mismatched values.
- `docs/DESIGN_SYSTEM.md` exists, is linked from `docs/MAP.md`, and the `/design-system` route matches it exactly (nothing documented that isn't live, nothing live that isn't documented).
- The Phase 6 static check runs in CI and reports zero findings (currently warn-only and non-zero — ratchets to hard-fail once Tier 3/5 clear the existing drift).
- The Konva canvas (`libs/pinyes-render`) draws its palette, surfaces, and node styling from the same token system as the rest of the app — no separate hardcoded set in `figure-palette.util.ts`.

---



## Appendix — Phase 1 Audit Findings

*Filled in as each audit step (1.1–1.4) completes.*

### 1.1 — Dashboard shared components vs. `docs/DASHBOARD_UI.md`

`apps/dashboard/src/app/shared/components/` has **19** components; the doc's "Components Compartits" table lists **11**. Eight exist in code but aren't documented:


| Component               | Category    | Notes                                                                                                            |
| ----------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------- |
| `color-picker`          | Forms       | Added 2026-08-12 (template editor tablet UX work) — undocumented                                                 |
| `header`                | Layout      | Shell chrome — the doc's Layout *diagram* mentions "Header" but the component table has no Layout section at all |
| `tab-nav`               | Layout      | Same — described in prose (icon+text/icon-only/hamburger), not in the table                                      |
| `user-chip`             | Layout      | Same                                                                                                             |
| `node-actions`          | Controls    | New category — undocumented entirely                                                                             |
| `node-dpad`             | Controls    | Same                                                                                                             |
| `privacy-consent-modal` | (top-level) | Not under `data/feedback/forms/layout` — its own top-level folder                                                |
| `tutorial-modal`        | (top-level) | Same                                                                                                             |


**Implication for the plan:** the doc's category scheme (Data/Feedback/Forms) doesn't cover what's actually in the tree — Layout and Controls are real, established categories missing from the table, and two components (`privacy-consent-modal`, `tutorial-modal`) don't fit any category, which is itself a signal `libs/ui`'s taxonomy (Phase 3) needs a clearer top-level structure than the dashboard grew organically.

**Style-rule compliance:** only one `.scss` file exists in the whole tree — `controls/node-dpad/node-dpad.component.scss` (80 lines, contains a transition/keyframe) — consistent with the documented "no `.scss` except animations" rule. No violations found here.

### 1.2 — PWA component landscape

The PWA has its own `shared/components/` (11 components) — but it's a **near-total fork**, not an extension, of the dashboard's. Comparing component names across both apps' `shared/components/` trees:

- **Shared name:** only `empty-state` — and the two implementations have already diverged (dashboard wraps content in a `card`, PWA doesn't; dashboard's icon input is a string name, PWA's is a `LucideIconData` object with a hardcoded `Calendar` default; the output event is named `actionClick` in one and `action` in the other). Same component, reinvented twice, already API-incompatible.
- **Conceptual duplicates under different names:** `privacy-consent-modal` (dashboard, 87 lines) vs. `consent-modal` (PWA, 87 lines) — same line count, independently built; `toast` + `toast.service.ts` (dashboard) vs. `toast-container` + a separate `toast.service.ts` under `shared/services/` (PWA) — two parallel toast systems.
- **PWA-only, no dashboard equivalent:** `bottom-tab-bar`, `mobile-header`, `no-person-banner`, `person-data-fields`, `person-switcher`, `pill-badge`, `pull-to-refresh`, `skeleton-card`, `splash-screen`.
- **Dashboard-only, no PWA equivalent:** all 18 others from 1.1 (data-table, filter-bar, pagination, etc. — mostly because the PWA has no admin-style list views).

**Hand-rolled patterns with no shared abstraction, either side:**

- Dashboard: 75 inline `class="badge ..."` usages across the app, with **no shared badge component** — every call site repeats DaisyUI's badge modifier classes directly. (Ironically the PWA *does* abstract this, as `pill-badge` — one more app-only reinvention, just in the other direction.)
- PWA: 9 features independently repeat `class="card bg-base-100 shadow-*"` as the card shell (`auth/login`, `auth/activate`, `auth/forgot-password`, `home`, `profile/settings`, …) instead of a shared card primitive.

**Implication for the plan:** this is stronger evidence than expected for Phase 3 — it's not "PWA has nothing, dashboard has everything," it's two independently-evolving component sets with real, silent API drift on the one component they do share. `libs/ui`'s migration order (3.2) should treat `empty-state`, the two modals, and the two toast systems as priority reconciliation targets, not just ports — their behavior needs to be diffed and unified, not copy-pasted.

### 1.3 — Drift quantification

Counts below exclude `.spec.ts` files (test fixtures, not rendered UI) and exclude `libs/pinyes-render` (covered separately in 1.4, since it's a distinct, already-known case).

**Raw hex codes** (`#rgb`/`#rrggbb`/`#rrggbbaa` literals in `.ts`/`.html`):


|           | Occurrences | Files |
| --------- | ----------- | ----- |
| Dashboard | 61          | 20    |
| PWA       | 4           | 2     |


These split into two real categories, not one:

- **Untokened fallback/decoration values** — e.g. `rengla-overlay.component.html`'s SVG `stroke="#6366f1"`/`fill="#6366f1"` (a pure UI highlight color, no domain meaning), `template-editor.component.html`'s legend swatch `background-color: #EEEEEE; border: 1px dashed #a1a1a1`, and fallback defaults like `[style.background-color]="pos.color ?? '#888'"` / `'#e5e7eb'` / `'#ccc'` scattered across `attendance-edit-modal`, `person-detail`, `template-editor`. These are exactly what token references replace.
- **Domain-color seams** — `tag-form-modal.component.ts`'s `DEFAULT_COLOR = '#6366f1'` and preset seed `'#64748b'`, and `pinyes/utils/node-color-presets.util.ts`. Tags and figure-node presets are user-assignable colors (legitimate domain data, not decoration) — but their *default/seed* values and the color-picker swatches presenting them should still be defined once, from the token set, not hardcoded independently of it. Same category as the canvas's categorical palette (1.4) — a "curated set of distinguishable colors" concern that belongs in the token layer per Phase 2.1's new Categorical row, not per-component.

**Arbitrary-value Tailwind classes** (`bg-[...]`, `w-[...]`, `text-[10px]`, `z-[9999]`, etc.) — a different, narrower drift signal than the documented "no dynamic classes" rule (that rule targets runtime-interpolated class strings; these are static but bypass the design system's scale entirely):


|           | Occurrences | Files |
| --------- | ----------- | ----- |
| Dashboard | 33          | 17    |
| PWA       | 6           | 6     |


Mostly one-off pixel/viewport sizes that should resolve to spacing-scale tokens once one exists (`max-h-[45vh]`, `w-[280px]`, `min-w-[10rem]`, `text-[10px]` appearing identically in 4 separate files as a de facto "extra-small text" size nobody named), plus two `z-[9999]` values (dashboard's `toast.component.ts`, PWA's `toast-container.component.ts`/`splash-screen.component.ts`) that are exactly the kind of thing a named z-index scale (Phase 2.1) exists to fix.

**Stray** `.scss` beyond the two expected app-root stylesheets (`styles.scss`, `app.scss`):


|           | Files | Notes                                                                                                                                                                                                                                                                                                                                      |
| --------- | ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Dashboard | 2     | `template-editor.component.scss` (332 lines, legitimately animation-heavy — complies with the documented exception) and `template-list.component.scss` (5 lines, a single `&:hover { opacity: 1 }` rule — **not** animation, and directly expressible as Tailwind's `group`/`group-hover:opacity-100` utilities; this one shouldn't exist) |
| PWA       | 6     | `delegations-modal`, `profile`, `settings`, `mobile-header`, `person-switcher`, `pill-badge` — **all 6 are empty (0 bytes)** but still wired via `styleUrl` in their component, directly contradicting the documented rule "Ometre `styleUrls` si no cal"                                                                                  |


**Implication for the plan:** the PWA's 6 dead `styleUrl` references are a trivial cleanup, worth doing during Phase 3 migration regardless of the design system work. The two Tailwind-drift categories (arbitrary values, untokened hex) are precisely what Phase 6's static check (6.1) is designed to catch — these counts are the "baseline" that check should validate against zero once Phases 2–3 land, per the plan's success criteria.

### 1.4 — Non-DOM color consumers (the Konva canvas)

**This finding revises Phase 2.3's framing.** The plan (and the Problem section above) named `figure-palette.util.ts` as *the* hardcoded canvas palette. It's actually a small fraction of it — a full sweep of `libs/pinyes-render/src` for hex literals (excluding specs) finds **98**, distributed like this:


| File                                                                                                      | Count | What it is                                                                      |
| --------------------------------------------------------------------------------------------------------- | ----- | ------------------------------------------------------------------------------- |
| `figure-canvas.component.ts`                                                                              | 64    | Konva draw calls — the actual canvas rendering                                  |
| `figure-palette.util.ts`                                                                                  | 12    | The documented qualitative figure-distinguishing palette                        |
| `figure-canvas.component.scss`                                                                            | 8     | DOM chrome around the canvas (wrapper, zoom selector, label editor, fit button) |
| `tronc-view.component.ts`/`.scss`, `own-position-marker.component.html`, `person-hover-card.component.ts` | 14    | Scattered UI-state colors in the surrounding rendering components               |


`figure-canvas.component.ts`**'s 64 values break into distinct semantic groups, all currently module-level** `const` **hex strings**, e.g.:

```ts
const GRID_COLOR = '#e5e7eb';
const NODE_COLORS: Record<string, string> = {
  [FigureZone.BASE]: '#EEEEEE',
  [FigureZone.PINYA]: '#3b82f6',
  [FigureZone.TRONC]: '#8b5cf6',
  [FigureZone.FIGURE_DIRECTION]: '#d97706',
  [FigureZone.XICALLA_DIRECTION]: '#db2777',
  [FigureZone.DECORATION]: '#999999',
};
const SELECTED_STROKE = '#f59e0b';
const NORMAL_STROKE = '#1e1b4b';
const CONFLICT_STROKE = '#e11d48';
// ...and inline, further down, per-render:
const ATTENDANCE_COLORS: Record<string, string> = {
  ANIRE: past ? '#f59e0b' : '#22c55e',
  ASSISTIT: '#22c55e',
  PENDENT: past ? '#ef4444' : '#f59e0b',
  NO_VAIG: '#ef4444',
};
```

Four groups, each a different design-system concern:

1. **Zone/domain colors** (`NODE_COLORS`) — which figure-node type gets which color. Genuinely domain-driven, but still needs *values* from the identity, the same way `node-color-presets.util.ts` (1.3) does.
2. **UI-state strokes** (`SELECTED_STROKE`, `NORMAL_STROKE`, `CONFLICT_STROKE`, plus an inline `'#10b981'` for highlighted state) — these are exactly what semantic tokens (selection/focus/warning/error) are for.
3. **Status colors reinventing existing tokens** (`ATTENDANCE_COLORS`) — `#22c55e`/`#f59e0b`/`#ef4444` are Tailwind's green-500/amber-500/red-500, i.e. functionally DaisyUI's `success`/`warning`/`error` **redefined from scratch in Konva-land**, independent of and only coincidentally matching the actual `success`/`warning`/`error` tokens already generated by `generateCollaTheme()` a few files away.
4. **Generic chrome** (white/black/gray fills and text) — ordinary surface/content colors, no different from any DOM `base-100`/`base-content` usage.

**The DOM-reachable** `.scss` **file is a microcosm of the whole plan.** `figure-canvas.component.scss` already *partially* uses DaisyUI's primary token (`border-color: oklch(var(--p))`, `box-shadow: 0 0 0 2px oklch(var(--p) / 0.2)` on `:focus`) sitting right next to fully hardcoded hex two lines away (`background-color: #f8fafc`, `border: 1px solid #e5e7eb`) for the same component's `:hover` state. Token adoption exists here in the one place it's technically trivial (plain CSS) and is still inconsistent — strong evidence that without Phase 6's guardrail, a token layer alone doesn't self-enforce.

**Implication for the plan:** Phase 2.3's TS-token-bridge module needs to cover meaningfully more ground than implied — not just "the palette," but zone colors, UI-state strokes, and status colors currently duplicating DaisyUI semantics independently. Recommend `ATTENDANCE_COLORS` be rebuilt to reference the *same* success/warning/error token values DaisyUI already generates (via the 2.3(a) bridge) rather than its own literals — one definition of "what error looks like," not two that happen to currently agree.

---

*Veïns: [[DASHBOARD_UI]] · [[MAP]] · [[DEBT]]*