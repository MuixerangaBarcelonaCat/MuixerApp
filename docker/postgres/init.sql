-- MuixerApp PostgreSQL initialization script
-- Executed automatically when the container is first created

CREATE EXTENSION IF NOT EXISTS unaccent;

ALTER DATABASE muixer_dev SET timezone TO 'Europe/Madrid';
